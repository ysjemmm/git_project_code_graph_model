现在有个 bug 信息，如下：
> 仓库信息：`.cache/git_repos/esign-egress-gateway`
> 问题：配置了固定限流数目，当存在接口超时时，后续所有请求一直会被限流

现在你是一个 **AI Bugfix 专家**，可以使用「代码图谱」+「本地代码仓库」来分析这个问题的根因，并给出可落地的修复方案。

为了对比不同信息源对排查效率与质量的影响，你需要在 **三种信息可见性模式** 下分别完成一次完整的排查与修复过程：

1. **仅看仓库代码**：只能访问本地代码仓库，不可使用代码图谱
2. **仅看代码图谱**：只能访问代码图谱中的结构/关系/调用信息，不可直接看源码
3. **同时看图谱 + 仓库**：可以同时使用代码图谱和本地代码仓库

在每一种模式下，你都需要输出一份 **排查全链路文档**，文档中至少包含：

- **问题重述**：用你自己的话重新描述 bug 现象与触发场景
- **初始假设**：基于现象你最先怀疑的组件/模块/原因
- **信息收集路径**：你依次查看了哪些文件 / 图谱节点 / 关系 / 查询结果
- **关键证据**：让你逐步排除/确认假设的核心证据（代码片段、调用链、配置项等）
- **根因定位**：最终确认的根因（包括：为什么会出现“接口超时后，所有请求一直被限流”这种表现）
- **修复方案**：
  - 修复思路与具体改动（配置、代码、限流策略、超时/熔断逻辑等）
  - 对下游调用方/运维/配置的影响
  - 需要补充的测试用例（单测/集成测试/压测场景等）
- **剩余风险与观察点**：修复上线后仍需重点观察的指标或边界场景

然后，你需要写一份 **总结与定量对比文档**，内容包含但不限于：

- **效率对比**：三种模式下从接到问题到定位根因的用时估计（分钟级）
- **导航成本**：
  - 查阅过的文件/节点/关系数量
  - 回溯/走错方向的次数
- **洞察深度**：
  - 是否能快速看出跨模块调用链、配置与代码不一致等问题
  - 是否更容易发现潜在的旁路问题（如：限流策略设计瑕疵、超时/熔断与限流的耦合问题）
- **修复质量**：
  - 修复方案覆盖的场景完整度
  - 是否自然地产生了测试与监控的改进建议
- **主观体验**：
  - 使用体验评分（1–10 分）：仅仓库 / 仅图谱 / 图谱+仓库
  - 你认为代码图谱在本次排查中最有价值的 2–3 点
  - 你认为仅看仓库最吃力的 2–3 点

最终目标：通过这一套 bug 排查实验，**定量 + 定性** 展示「代码图谱」相比单看仓库，在真实复杂问题排查中的效率与质量优势。