<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.timevale.forward.dal.dao.ViewsMapper">

    <!-- 新增一条视图 -->
    <insert id="insert" parameterType="com.timevale.forward.dal.entity.ViewsDO" useGeneratedKeys="true" keyProperty="id" keyColumn="id">
        INSERT INTO views
        (name, `type`, owner, owner_id, create_man_id, create_man, group_field, filter_condition)
        VALUES
        (#{name}, #{type}, #{owner}, #{ownerId}, #{createManId}, #{createMan}, #{groupField}, #{filterCondition})
    </insert>

    <!-- 根据主键id获取视图 -->
    <select id="get" resultType="com.timevale.forward.dal.entity.ViewsDO">
        SELECT * FROM views WHERE id=#{id} AND is_deleted = 0
    </select>

    <!-- 根据业务域id和名称查询视图 -->
    <select id="getByTypeAndName" resultType="com.timevale.forward.dal.entity.ViewsDO">
        SELECT * FROM views WHERE `type` = #{type} AND name = #{name} AND is_deleted = 0
    </select>

    <!-- 更新视图 -->
    <update id="update">
        UPDATE views
        <set>
            <if test="name != null">name = #{name},</if>
            <if test="owner != null">owner = #{owner},</if>
            <if test="ownerId != null">owner_id = #{ownerId},</if>
            <if test="groupField != null">group_field = #{groupField},</if>
            <if test="filterCondition != null">filter_condition = #{filterCondition},</if>
            <if test="modifyManId != null">modify_man_id = #{modifyManId},</if>
            <if test="modifyMan != null">modify_man = #{modifyMan},</if>
            <if test="modifyDate != null">modify_date = #{modifyDate},</if>
        </set>
        WHERE id = #{id} and is_deleted = false
    </update>

    <!-- 删除视图 -->
    <update id="delete">
        UPDATE views
        SET is_deleted = true, modify_man_id = #{modifyManId}, modify_man = #{modifyMan}
        WHERE id = #{id} and is_deleted = false
    </update>

</mapper>