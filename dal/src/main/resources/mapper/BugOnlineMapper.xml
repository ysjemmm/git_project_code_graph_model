<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.timevale.forward.dal.dao.BugOnlineMapper">
    <select id="selectById" resultType="com.timevale.forward.dal.entity.BugOnlineDO">
        select *
        from bug_online
        where id = #{id}
          and is_deleted = 0
    </select>

    <select id="selectListByCondition" resultType="com.timevale.forward.dal.entity.BugOnlineListDO">
        select bo.id, bo.name, bo.status, bo.operator, bo.operator_id, bo.proposer, bo.proposer_id, bo.priority, bo.env,
        bo.reason, bo.belong, bo.dismiss_cause, bo.customer_name, bo.expect_launch_date, bo.source,
        bo.create_date, bo.modify_date,bo.area
        from bug_online bo
        left join bug_online_product_line bopl on bo.id = bopl.bug_online_id AND bopl.type =0  AND bopl.is_deleted = false
        left join bug_online_model mo on bo.id = mo.bug_online_id and mo.is_deleted = false
        left join product_line pl on bopl.product_line_id = pl.id
        left join biz_domain bdm on pl.biz_domain_id = bdm.id

        <if test="isSystemCloseBug!=null and isSystemCloseBug==true">
        left join bug_online_status_operator boso on bo.id = boso.bug_online_id and boso.is_deleted = false
        </if>
        <where>
            <if test="containIds!=null and containIds.size()>0">
                and bo.id in
                <foreach collection="containIds" item="item" separator="," open="(" close=")">
                    #{item}
                </foreach>
            </if>
            <if test="exclusiveIds!=null and exclusiveIds.size()>0">
                and bo.id not in
                <foreach collection="exclusiveIds" item="item" separator="," open="(" close=")">
                    #{item}
                </foreach>
            </if>
            <if test="name != null and name != '' ">
                and bo.name like concat('%',#{name},'%')
            </if>
            <if test="customerName!= null and customerName != '' ">
                and bo.customer_name like concat('%',#{customerName},'%')
            </if>
            <if test="idList != null and idList.size() > 0">
                and bo.id in
                <foreach collection="idList" item="item" separator="," open="(" close=")">
                    #{item}
                </foreach>
            </if>
            <if test="status != null and status.size() > 0">
                and bo.status in
                <foreach collection="status" item="item" separator="," open="(" close=")">
                    #{item}
                </foreach>
            </if>
            <if test="operatorIdList != null and operatorIdList.size() > 0">
                and bo.operator_id in
                <foreach collection="operatorIdList" item="item" separator="," open="(" close=")">
                    #{item}
                </foreach>
            </if>
            <if test="proposerIdList != null and proposerIdList.size() > 0">
                and bo.proposer_id in
                <foreach collection="proposerIdList" item="item" separator="," open="(" close=")">
                    #{item}
                </foreach>
            </if>
            <if test="priorities != null and priorities.size() > 0">
                and bo.priority in
                <foreach collection="priorities" item="item" separator="," open="(" close=")">
                    #{item}
                </foreach>
            </if>
            <if test="envs != null and envs.size() > 0">
                and bo.env in
                <foreach collection="envs" item="item" separator="," open="(" close=")">
                    #{item}
                </foreach>
            </if>
            <if test="reasons != null and reasons.size() > 0">
                and bo.reason in
                <foreach collection="reasons" item="item" separator="," open="(" close=")">
                    #{item}
                </foreach>
            </if>
            <if test="belongs != null and belongs.size() > 0">
                and bo.belong in
                <foreach collection="belongs" item="item" separator="," open="(" close=")">
                    #{item}
                </foreach>
            </if>
            <if test="sourceId != null and sourceId != ''">
                and bo.source_id = #{sourceId}
            </if>
            <if test="dismissCauseList != null and dismissCauseList.size() > 0">
                and bo.dismiss_cause in
                <foreach collection="dismissCauseList" item="item" separator="," open="(" close=")">
                    #{item}
                </foreach>
            </if>
            <if test="sourceList != null and sourceList.size() > 0">
                and bo.source in
                <foreach collection="sourceList" item="item" separator="," open="(" close=")">
                    #{item}
                </foreach>
            </if>
            <if test="areas != null and areas.size() > 0">
                and bo.area in
                <foreach collection="areas" item="item" separator="," open="(" close=")">
                    #{item}
                </foreach>
            </if>
            <if test="modelIds != null and modelIds.size() > 0">
                and mo.model_id in
                <foreach collection="modelIds" item="item" separator="," open="(" close=")">
                    #{item}
                </foreach>
            </if>
            <if test="productLineIdList != null and productLineIdList.size() > 0">
                and pl.id in
                <foreach collection="productLineIdList" item="item" separator="," open="(" close=")">
                    #{item}
                </foreach>
            </if>
            <if test="bizDomainIdList != null and bizDomainIdList.size() > 0">
                and bdm.id in
                <foreach collection="bizDomainIdList" item="item" separator="," open="(" close=")">
                    #{item}
                </foreach>
            </if>
            <if test="createDateLeft != null and createDateRight != null">
                and bo.create_date &gt;= #{createDateLeft} and bo.create_date &lt;= #{createDateRight}
            </if>
            <if test="modifyDateLeft != null and modifyDateRight != null">
                and bo.modify_date &gt;= #{modifyDateLeft} and bo.modify_date &lt;= #{modifyDateRight}
            </if>
            <if test="copier!=null and copier!='' ">
                and bo.id IN (
                select p.main_id
                from person p
                where p.user_id = #{copier}
                and p.type = 60
                and p.is_deleted = false
                )
            </if>
            <if test="isSystemCloseBug!=null and isSystemCloseBug==true">
                and  bo.status =2 and boso.status=2 and boso.operator_id='SYSTEM'
             </if>
            <if test="true">
                and bo.is_deleted = false
                and bopl.is_deleted = false
            </if>
        </where>
        group by bo.id
    </select>

    <select id="selectByBizDemandId" resultType="com.timevale.forward.dal.entity.BugOnlineDO">
        SELECT id, name
        FROM bug_online
        WHERE biz_demand_id = #{bizDemandId}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="bugOnlineDO.id">
        insert into bug_online(name, operator, operator_id, last_operator, last_operator_id, proposer, proposer_id,
                               customer_name, env, belong, priority, recurrent, reason, expect_date, business,
                               `describe`, system_menu_name, create_man, create_man_id, source, detail_version_id,
                               source_id, customer_dev_project_name, biz_id, biz_name,area)
        values (#{bugOnlineDO.name}, #{bugOnlineDO.operator}, #{bugOnlineDO.operatorId}, #{bugOnlineDO.lastOperator},
                #{bugOnlineDO.lastOperatorId}, #{bugOnlineDO.proposer}, #{bugOnlineDO.proposerId},
                #{bugOnlineDO.customerName}, #{bugOnlineDO.env}, #{bugOnlineDO.belong}, #{bugOnlineDO.priority},
                #{bugOnlineDO.recurrent}, #{bugOnlineDO.reason}, #{bugOnlineDO.expectDate}, #{bugOnlineDO.business},
                #{bugOnlineDO.describe}, #{bugOnlineDO.systemMenuName}, #{createMan}, #{createManId},
                #{bugOnlineDO.source}, #{bugOnlineDO.detailVersionId}, #{bugOnlineDO.sourceId},
                #{bugOnlineDO.customerDevProjectName}, #{bugOnlineDO.bizId}, #{bugOnlineDO.bizName}, #{bugOnlineDO.area})
    </insert>

    <update id="update">
        update bug_online
        <set>
            <if test="bugOnlineDO.name != null">
                name = #{bugOnlineDO.name},
            </if>
            <if test="bugOnlineDO.operator != null">
                operator = #{bugOnlineDO.operator},
            </if>
            <if test="bugOnlineDO.operatorId != null">
                operator_id = #{bugOnlineDO.operatorId},
            </if>
            <if test="bugOnlineDO.lastOperator != null">
                last_operator = #{bugOnlineDO.lastOperator},
            </if>
            <if test="bugOnlineDO.lastOperatorId != null">
                last_operator_id = #{bugOnlineDO.lastOperatorId},
            </if>
            <if test="bugOnlineDO.proposer != null">
                proposer = #{bugOnlineDO.proposer},
            </if>
            <if test="bugOnlineDO.proposerId != null">
                proposer_id = #{bugOnlineDO.proposerId},
            </if>
            <if test="true">
                customer_name = #{bugOnlineDO.customerName},
            </if>
            <if test="bugOnlineDO.bizDemandId != null">
                biz_demand_id = #{bugOnlineDO.bizDemandId},
            </if>
            <if test="bugOnlineDO.env != null">
                env = #{bugOnlineDO.env},
            </if>
            <if test="bugOnlineDO.belong != null">
                belong = #{bugOnlineDO.belong},
            </if>
            <if test="bugOnlineDO.priority != null">
                priority = #{bugOnlineDO.priority},
            </if>
            <if test="bugOnlineDO.recurrent != null">
                recurrent = #{bugOnlineDO.recurrent},
            </if>
            <if test="true">
                reason = #{bugOnlineDO.reason},
            </if>
            <if test="true">
                expect_date = #{bugOnlineDO.expectDate},
            </if>
            <if test="bugOnlineDO.business != null">
                business = #{bugOnlineDO.business},
            </if>
            <if test="bugOnlineDO.describe != null">
                `describe` = #{bugOnlineDO.describe},
            </if>
            <if test="bugOnlineDO.prevStatus != null">
                prev_status = #{bugOnlineDO.prevStatus},
            </if>
            <if test="bugOnlineDO.hangUp != null">
                hang_up = #{bugOnlineDO.hangUp},
            </if>
            <if test="bugOnlineDO.status != null">
                status = #{bugOnlineDO.status},
            </if>
            <if test="true">
                dismiss_cause = #{bugOnlineDO.dismissCause},
            </if>
            <if test="bugOnlineDO.problemReason != null">
                problem_reason = #{bugOnlineDO.problemReason},
            </if>
            <if test="bugOnlineDO.solveScheme != null">
                solve_scheme = #{bugOnlineDO.solveScheme},
            </if>
            <if test="true">
                repair_fail_reason = #{bugOnlineDO.repairFailReason},
            </if>
            <if test="bugOnlineDO.openAgainReason != null">
                open_again_reason = #{bugOnlineDO.openAgainReason},
            </if>
            <if test="bugOnlineDO.isDeleted != null">
                is_deleted = #{bugOnlineDO.isDeleted},
            </if>
            <if test="bugOnlineDO.detailVersionId != null">
                detail_version_id = #{bugOnlineDO.detailVersionId},
            </if>
            <if test="bugOnlineDO.expectLaunchDate != null">
                expect_launch_date = #{bugOnlineDO.expectLaunchDate},
            </if>
            <if test="bugOnlineDO.sourceId != null and bugOnlineDO.sourceId != ''">
                source_id = #{bugOnlineDO.sourceId},
            </if>
            <if test="bugOnlineDO.customerDevProjectName != null and bugOnlineDO.customerDevProjectName != ''">
                customer_dev_project_name = #{bugOnlineDO.customerDevProjectName},
            </if>
            <if test="true">
                area = #{bugOnlineDO.area},
            </if>
            <if test="true">
                modify_man_id = #{modifyManId},
                modify_man = #{modifyMan}
            </if>
        </set>
        <where>
            id = #{bugOnlineDO.id}
        </where>
    </update>

    <update id="fullUpdate">
        update bug_online
        <set>
            name = #{bugOnlineDO.name},
            operator = #{bugOnlineDO.operator},
            operator_id = #{bugOnlineDO.operatorId},
            last_operator = #{bugOnlineDO.lastOperator},
            last_operator_id = #{bugOnlineDO.lastOperatorId},
            proposer = #{bugOnlineDO.proposer},
            proposer_id = #{bugOnlineDO.proposerId},
            customer_name = #{bugOnlineDO.customerName},
            biz_demand_id = #{bugOnlineDO.bizDemandId},
            env = #{bugOnlineDO.env},
            belong = #{bugOnlineDO.belong},
            priority = #{bugOnlineDO.priority},
            recurrent = #{bugOnlineDO.recurrent},
            reason = #{bugOnlineDO.reason},
            expect_date = #{bugOnlineDO.expectDate},
            business = #{bugOnlineDO.business},
            `describe` = #{bugOnlineDO.describe},
            prev_status = #{bugOnlineDO.prevStatus},
            hang_up = #{bugOnlineDO.hangUp},
            `status` = #{bugOnlineDO.status},
            dismiss_cause = #{bugOnlineDO.dismissCause},
            problem_reason = #{bugOnlineDO.problemReason},
            solve_scheme = #{bugOnlineDO.solveScheme},
            repair_fail_reason = #{bugOnlineDO.repairFailReason},
            open_again_reason = #{bugOnlineDO.openAgainReason},
            is_deleted = #{bugOnlineDO.isDeleted},
            detail_version_id = #{bugOnlineDO.detailVersionId},
            modify_man_id = #{modifyManId},
            modify_man = #{modifyMan}
        </set>
        <where>
            id = #{bugOnlineDO.id}
        </where>
    </update>

    <select id="selectByLinkBugId" resultType="com.timevale.forward.dal.entity.BugOnlineDO">
        SELECT id
             , name
             , operator
             , operator_id
             , last_operator
             , last_operator_id
             , proposer
             , proposer_id
             , customer_name
             , env
             , belong
             , priority
             , recurrent
             , reason
             , expect_date
             , business
             , `describe`
             , system_menu_name
             , create_man
             , create_man_id
             , source
             , detail_version_id
             , link_bug_id
        FROM bug_online
        WHERE link_bug_id = #{linkBugId}
          and is_deleted = false
    </select>

    <update id="updateByIds">
        update bug_online
        <set>
            link_bug_id =#{linkBugId}
        </set>
        <where>
            and id IN
            <foreach item="item" index="index" collection="ids" open="(" separator="," close=")">
                #{item}
            </foreach>
        </where>
    </update>

    <select id="selectByIds" resultType="com.timevale.forward.dal.entity.BugOnlineDO">
        SELECT id, name,operator,operator_id,last_operator,last_operator_id,proposer
        ,proposer_id,customer_name,env,belong,priority,recurrent,reason,expect_date,business,`describe`,system_menu_name
        ,create_man,create_man_id,source, detail_version_id,link_bug_id
        FROM bug_online
        <where>
            id in
            <foreach collection="ids" item="item" separator="," open="(" close=")">
                #{item}
            </foreach>
            <if test="containDeleted == false">
                and is_deleted = false
            </if>
        </where>
    </select>

    <select id="selectByName" resultType="com.timevale.forward.dal.entity.BugOnlineDO">
        SELECT id, name,operator,operator_id,last_operator,last_operator_id,proposer
        ,proposer_id,customer_name,env,belong,priority,recurrent,reason,expect_date,business,`describe`,system_menu_name
        ,create_man,create_man_id,source, detail_version_id,link_bug_id
        FROM bug_online
        <where>
            name like concat('%',#{name},'%') and is_deleted = false
        </where>
    </select>

    <select id="selectByStatus" resultType="com.timevale.forward.dal.entity.BugOnlineDO">
        SELECT id, name,operator,operator_id,last_operator,last_operator_id,proposer
        ,proposer_id,customer_name,env,belong,priority,recurrent,reason,expect_date,business,`describe`,system_menu_name
        ,create_man,create_man_id,source, detail_version_id,link_bug_id
        ,is_deleted,create_man ,create_man_id,create_date ,modify_man ,modify_man_id,modify_date
        FROM bug_online
        <where>
            status in
            <foreach collection="status" item="item" separator="," open="(" close=")">
                #{item}
            </foreach>
            and is_deleted = false
        </where>
    </select>

    <update id="updateStatusByIds">
        update bug_online
        <set>
            status =#{status}
        </set>
        <where>
            and id IN
            <foreach item="item" index="index" collection="ids" open="(" separator="," close=")">
                #{item}
            </foreach>
        </where>
    </update>

    <select id="selectByCustomId" resultType="com.timevale.forward.dal.entity.BugOnlineListDO">
        select bo.id, bo.name, bo.status, bo.operator, bo.operator_id, bo.proposer, bo.proposer_id, bo.priority, bo.env,
        bo.reason, bo.belong, bo.dismiss_cause, bo.customer_name, bo.expect_launch_date, bo.source,
        bo.create_date, bo.modify_date
        from bug_online bo
         left join bug_online_custom bc on bo.id = bc.bug_online_id where bc.custom_id = #{customId} and bc.is_deleted=0 and bo.is_deleted=0 order by bo.create_date desc

    </select>

    <select id="selectAllId" resultType="java.lang.Long">
        SELECT id
        FROM bug_online
        WHERE is_deleted = false
    </select>
</mapper>
























