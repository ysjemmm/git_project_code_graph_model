<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<!--你接口的包名是com.abc.dao,接口名是NameMapper.java，那么你的mapper.xml的namespace应该是com.abc.dao.NameMapper-->
<mapper namespace="com.timevale.forward.dal.dao.ProjectMapper">

    <insert id="insert" parameterType="com.timevale.forward.dal.entity.ProjectDO" useGeneratedKeys="true" keyProperty="id" keyColumn="id">
        insert into project (name, priority, type ,status,pm,plan_start_date,plan_end_date,`desc`, create_man_id, create_man)
        values
        (#{name}, #{priority}, #{type},#{status}, #{pm},#{planStartDate},#{planEndDate},#{desc}, #{createManId}, #{createMan})
    </insert>

    <select id="get" resultType="com.timevale.forward.dal.entity.ProjectDO">
        select name, priority, type ,status,pm,plan_start_date,plan_end_date,`desc`, create_man_id, create_man,create_date,modify_date
        from project
        where id = #{id}
    </select>
</mapper>
