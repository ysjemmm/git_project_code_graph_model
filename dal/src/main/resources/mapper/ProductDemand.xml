<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<!--你接口的包名是com.abc.dao,接口名是NameMapper.java，那么你的mapper.xml的namespace应该是com.abc.dao.NameMapper-->
<mapper namespace="com.timevale.forward.dal.dao.ProductDemandMapper">

    <insert id="insert" parameterType="com.timevale.forward.dal.entity.ProductDemandDO" useGeneratedKeys="true"
            keyProperty="id" keyColumn="id">
        insert into product_demand (name, product_line_id, type, `desc`, status, priority, owner, owner_id,
                                    expect_schedule_time, target_customer, customer_grade, create_man_id, create_man, ued_time, transfer_time, product_time, back_time, front_time, qa_time, ops_time, security_time, total_time)
        values (#{name}, #{productLineId}, #{type}, #{desc}, #{status}, #{priority}, #{owner}, #{ownerId},
                #{expectScheduleTime}, #{targetCustomer}, #{customerGrade},#{createManId}, #{createMan}, #{uedTime}, #{transferTime}, #{productTime}, #{backTime}, #{frontTime}, #{qaTime}, #{opsTime}, #{securityTime}, #{totalTime})
    </insert>

    <insert id="batchUpsertProductDemandOwners">
        INSERT INTO product_demand_owner
        (product_demand_id, owner, owner_id, resource_type, create_man_id, create_man)
        VALUES
        <foreach collection="productDemandOwnerDoS" item="item" index="index" separator=",">
            (
            #{item.productDemandId},
            #{item.owner},
            #{item.ownerId},
            #{item.resourceType},
            #{operatorId},
            #{operator}
            )
        </foreach>
        ON DUPLICATE KEY UPDATE
        owner = IFNULL(VALUES(owner), owner),
        owner_id = IFNULL(VALUES(owner_id), owner_id),
        resource_type = IFNULL(VALUES(resource_type), resource_type),
        modify_man_id = #{operatorId},
        modify_man = #{operator},
        modify_date = CURRENT_TIMESTAMP,
        is_deleted = 0
    </insert>

    <select id="list" resultType="com.timevale.forward.dal.entity.ProductDemandListDO">
        select a.id,a.name,a.priority,a.owner,a.owner_id,a.status, a.product_line_id,
        a.create_man,a.create_man_id,a.create_date,a.modify_date, a.type, a.expect_schedule_time,
        c.name productLineName ,d.name bizDomainName, a.ued_time, a.transfer_time, a.back_time,
        a.front_time, a.qa_time, a.ops_time, a.security_time, a.total_time, a.target_customer, a.customer_grade
        <if test="copierId != null and copierId != ''">
            ,e.user_name copier
        </if>
        from product_demand a
        left join product_line c on a.product_line_id=c.id
        left join biz_domain d on c.biz_domain_id=d.id
        <if test="copierId != null and copierId != ''">
            left join person e on a.id=e.main_id and e.type =20 and e.is_deleted =false
        </if>
        <where>
            <if test="id != null">and a.id = #{id}</if>
            <if test="name != null and name != ''">and a.name like concat('%',#{name},'%')</if>
            <if test="copierId != null and copierId != ''">and e.user_id = #{copierId}</if>
            <if test="createDateStart != null and createDateEnd != null">
                and a.create_date &gt;= #{createDateStart} and a.create_date &lt;= #{createDateEnd}
            </if>
            <if test="modifyDateStart != null and modifyDateEnd != null">
                and a.modify_date &gt;= #{modifyDateStart} and a.modify_date &lt;= #{modifyDateEnd}
            </if>
            <if test="expectScheduleTimeStart != null and expectScheduleTimeEnd != null">
                and a.expect_schedule_time &gt;= #{expectScheduleTimeStart} and a.expect_schedule_time &lt;=
                #{expectScheduleTimeEnd}
            </if>
            <if test="priorities!=null and priorities.size()>0">
                and a.priority in
                <foreach collection="priorities" item="item" separator="," open="(" close=")">
                    #{item}
                </foreach>
            </if>
            <if test="productLineIds!=null and productLineIds.size()>0">
                and c.id in
                <foreach collection="productLineIds" item="item" separator="," open="(" close=")">
                    #{item}
                </foreach>
            </if>
            <if test="bizDomainIds!=null and bizDomainIds.size()>0">
                and d.id in
                <foreach collection="bizDomainIds" item="item" separator="," open="(" close=")">
                    #{item}
                </foreach>
            </if>
            <if test="ownerIds!=null and ownerIds.size()>0">
                and a.owner_id in
                <foreach collection="ownerIds" item="item" separator="," open="(" close=")">
                    #{item}
                </foreach>
            </if>
            <if test="status!=null and status.size()>0">
                and a.status in
                <foreach collection="status" item="item" separator="," open="(" close=")">
                    #{item}
                </foreach>
            </if>
            <if test="filterProductDemandIds!=null and filterProductDemandIds.size()>0">
                and a.id not in
                <foreach collection="filterProductDemandIds" item="item" separator="," open="(" close=")">
                    #{item}
                </foreach>
            </if>
            <if test="inProductDemandIds!=null and inProductDemandIds.size()>0">
                and a.id in
                <foreach collection="inProductDemandIds" item="item" separator="," open="(" close=")">
                    #{item}
                </foreach>
            </if>
            <if test="notInProductDemandIds!=null and notInProductDemandIds.size()>0">
                and a.id not in
                <foreach collection="notInProductDemandIds" item="item" separator="," open="(" close=")">
                    #{item}
                </foreach>
            </if>
            <if test="types!= null and types!='' ">
                and a.type REGEXP CONCAT('[', #{types},']')
            </if>
            and a.is_deleted =false
        </where>
    </select>

    <select id="selectByIdList" resultType="com.timevale.forward.dal.entity.ProductDemandDO">
        select id, name, product_line_id, type, `desc`, status, priority, owner, is_deleted, create_man_id, create_man,
        create_date, modify_man_id, modify_man, modify_date, owner_id, ued_time, product_time, back_time, front_time, qa_time, ops_time, security_time, total_time
        from product_demand
        where
        is_deleted = false
        and id in
        <foreach collection="productDemandIdList" item="item" separator="," open="(" close=")">
            #{item}
        </foreach>
    </select>

    <select id="selectList" resultType="com.timevale.forward.dal.entity.ProductDemandDO">
        select *
        from product_demand
        where
        is_deleted = false and (customer_grade is null or customer_grade = '')
    </select>

    <select id="selectByBizDemandId" resultType="com.timevale.forward.dal.entity.BizDemandLinkProductDemandListDO">
        select pd.id,
               pd.name,
               pd.type,
               pd.`desc`,
               pd.status,
               pd.priority,
               pd.owner,
               pd.is_deleted,
               pd.create_man_id,
               pd.create_man,
               pd.create_date,
               pd.modify_man_id,
               pd.modify_man,
               pd.modify_date,
               pd.owner_id
        from product_demand pd
                 left join product_biz_demand pbd on pd.id = pbd.product_demand_id
        where pbd.biz_demand_id = #{bizDemandId}
          and pbd.is_deleted = false
    </select>

    <select id="selectById" resultType="com.timevale.forward.dal.entity.ProductDemandDO">
        select id,
               name,
               product_line_id,
               type,
               `desc`,
               status,
               priority,
               owner,
               is_deleted,
               create_man_id,
               create_man,
               create_date,
               modify_man_id,
               modify_man,
               modify_date,
               owner_id,
               ued_time,
               transfer_time,
               back_time,
               front_time,
               qa_time,
               ops_time,
               security_time,
               total_time,
               target_customer,
               customer_grade
        from product_demand
        where id = #{id}
    </select>

    <select id="linkProductDemandList" resultType="com.timevale.forward.dal.entity.ProductDemandListDO">
        SELECT a.id,
               a.name,
               a.priority,
               a.owner,
               a.status,
               a.create_man,
               a.create_date,
               b.modify_date,
               a.desc
        FROM product_demand a
                 LEFT JOIN project_product_demand b ON a.id = b.product_demand_id AND b.is_deleted = false
        <where>
            b.project_id = #{projectId}
            <if test="demandName!=null and demandName!=''">
                and a.name like concat('%',#{demandName},'%')
            </if>
        </where>
        order by modify_date desc, id
    </select>

    <select id="selectListOfBizDemandLink"
            resultType="com.timevale.forward.dal.entity.BizDemandLinkProductDemandListDO">
        select pd.id, pd.name, pd.status, pd.priority, pd.owner, pd.owner_id,
        pd.is_deleted, pd.create_man_id, pd.create_man, pd.create_date, pd.modify_man_id, pd.modify_man, pd.modify_date,
        pl.id productLineId, pl.name productLineName,
        bd.id bizDomainId, bd.name bizDomainName
        from product_demand pd
        left join product_line pl on pd.product_line_id = pl.id
        left join biz_domain bd on pl.biz_domain_id = bd.id
        <where>
            and pd.status != -20
            <if test="containIds!=null and containIds.size()>0">
                and pd.id in
                <foreach collection="containIds" item="item" separator="," open="(" close=")">
                    #{item}
                </foreach>
            </if>
            <if test="productDemandId!=null">
                and pd.id = #{productDemandId}
            </if>
            <if test="name!=null and name!=''">
                and pd.name like concat('%',#{name},'%')
            </if>
            <if test="priorityList!=null and priorityList.size()>0">
                and pd.priority in
                <foreach collection="priorityList" item="item" separator="," open="(" close=")">
                    #{item}
                </foreach>
            </if>
            <if test="statusList!=null and statusList.size()>0">
                and pd.status in
                <foreach collection="statusList" item="item" separator="," open="(" close=")">
                    #{item}
                </foreach>
            </if>
            <if test="bizDomainIdList!=null and bizDomainIdList.size()>0">
                and pl.biz_domain_id in
                <foreach collection="bizDomainIdList" item="item" separator="," open="(" close=")">
                    #{item}
                </foreach>
            </if>
            <if test="productLineIdList!=null and productLineIdList.size()>0">
                and pd.product_line_id in
                <foreach collection="productLineIdList" item="item" separator="," open="(" close=")">
                    #{item}
                </foreach>
            </if>
            <if test="ownerIdList!=null and ownerIdList.size()>0">
                and pd.owner_id in
                <foreach collection="ownerIdList" item="item" separator="," open="(" close=")">
                    #{item}
                </foreach>
            </if>
            <if test="linkedIdList!=null and linkedIdList.size()>0">
                and pd.id not in
                <foreach collection="linkedIdList" item="item" separator="," open="(" close=")">
                    #{item}
                </foreach>
            </if>
            <choose>
                <when test="createDateStart!=null and createDateEnd!=null">
                    and pd.create_date &gt;= #{createDateStart}
                    and pd.create_date &lt;= #{createDateEnd}
                </when>
                <otherwise>
                    <choose>
                        <when test="createDateStart!=null">
                            and pd.create_date &gt;= #{createDateStart}
                        </when>
                        <when test="createDateEnd!=null">
                            and pd.create_date &lt;= #{createDateEnd}
                        </when>
                    </choose>
                </otherwise>
            </choose>
        </where>
    </select>

    <update id="update" parameterType="com.timevale.forward.dal.entity.ProductDemandDO">
        update product_demand
        <set>
            <if test="name != null">name = #{name},</if>
            <if test="productLineId != null">product_line_id = #{productLineId},</if>
            <if test="owner != null">owner = #{owner},</if>
            <if test="ownerId != null">owner_id = #{ownerId},</if>
            <if test="priority != null">priority = #{priority},</if>
            <if test="type != null">type = #{type},</if>
            <if test="status != null">status = #{status},</if>
            <if test="desc != null">`desc` = #{desc},</if>
            <if test="expectScheduleTime != null">expect_schedule_time = #{expectScheduleTime},</if>
            <if test="targetCustomer != null">target_customer = #{targetCustomer},</if>
            <if test="customerGrade != null">customer_grade = #{customerGrade},</if>
            <if test="modifyMan != null">modify_man = #{modifyMan},</if>
            <if test="modifyManId != null">modify_man_id = #{modifyManId},</if>
            <if test="uedTime != null">ued_time = #{uedTime},</if>
            <if test="frontTime != null">front_time = #{frontTime},</if>
            <if test="backTime != null">back_time = #{backTime},</if>
            <if test="qaTime != null">qa_time = #{qaTime},</if>
            <if test="opsTime != null">ops_time = #{opsTime},</if>
            <if test="securityTime != null">security_time = #{securityTime},</if>
            <if test="transferTime != null">transfer_time = #{transferTime},</if>
            <if test="productTime != null">product_time = #{productTime},</if>
            <if test="totalTime != null">total_time = #{totalTime},</if>
        </set>
        <where>
            id = #{id} and is_deleted=false
        </where>
    </update>

    <update id="updateStatus" parameterType="com.timevale.forward.dal.entity.ProductDemandDO">
        update product_demand
        <set>
            <if test="status != null">status = #{status},</if>
            <if test="modifyMan != null">modify_man = #{modifyMan},</if>
            <if test="modifyManId != null">modify_man_id = #{modifyManId},</if>
            <if test="modifyDate != null">modify_date = #{modifyDate}</if>
        </set>
        <where>
            id = #{id} and is_deleted=false
        </where>
    </update>

    <update id="updateResourcePlan" parameterType="com.timevale.forward.dal.entity.ProductDemandDO">
        update product_demand
        <set>
            <if test="modifyMan != null">modify_man = #{modifyMan},</if>
            <if test="modifyManId != null">modify_man_id = #{modifyManId},</if>
            <if test="uedTime != null">ued_time = #{uedTime},</if>
            <if test="frontTime != null">front_time = #{frontTime},</if>
            <if test="backTime != null">back_time = #{backTime},</if>
            <if test="qaTime != null">qa_time = #{qaTime},</if>
            <if test="opsTime != null">ops_time = #{opsTime},</if>
            <if test="securityTime != null">security_time = #{securityTime},</if>
            <if test="transferTime != null">transfer_time = #{transferTime},</if>
            <if test="productTime != null">product_time = #{productTime}</if>
        </set>
        <where>
            id = #{id} and is_deleted=false
        </where>
    </update>

    <select id="get" resultType="com.timevale.forward.dal.entity.ProductDemandDO">
        select *
        from product_demand a
        where a.id = #{id}
          and a.is_deleted = false
    </select>

    <update id="updateByIds">
        update product_demand
        <set>
            <if test="retainModifyDate">modify_date = modify_date,</if>
            status =#{status}
        </set>
        <where>
            and id IN
            <foreach item="item" index="index" collection="ids" open="(" separator="," close=")">
                #{item}
            </foreach>
        </where>
    </update>

    <update id="updateOwner">
        UPDATE product_demand
        SET owner = #{owner},
        owner_id = #{ownerId}
        WHERE id IN
        <foreach collection="idList" item="item" separator="," open="(" close=")">
            #{item}
        </foreach>
    </update>

    <update id="batchDeleteProductDemandOwners">
        UPDATE product_demand_owner
        SET is_deleted = 1,
        modify_man_id = #{operatorId},
        modify_man = #{operator},
        modify_date = NOW()
        WHERE id IN
        <foreach collection="ids" item="id" separator="," open="(" close=")">
            #{id}
        </foreach>
    </update>

    <update id="updateResourceTime">
        UPDATE product_demand
        <trim prefix="SET" suffixOverrides=",">
            <trim prefix="ued_time = CASE" suffix="END,">
                <foreach collection="demands" item="d">
                    WHEN id = #{d.id} THEN #{d.uedTime}
                </foreach>
            </trim>
            <trim prefix="front_time = CASE" suffix="END,">
                <foreach collection="demands" item="d">
                    WHEN id = #{d.id} THEN #{d.frontTime}
                </foreach>
            </trim>
            <trim prefix="back_time = CASE" suffix="END,">
                <foreach collection="demands" item="d">
                    WHEN id = #{d.id} THEN #{d.backTime}
                </foreach>
            </trim>
            <trim prefix="qa_time = CASE" suffix="END,">
                <foreach collection="demands" item="d">
                    WHEN id = #{d.id} THEN #{d.qaTime}
                </foreach>
            </trim>
            <trim prefix="ops_time = CASE" suffix="END,">
                <foreach collection="demands" item="d">
                    WHEN id = #{d.id} THEN #{d.opsTime}
                </foreach>
            </trim>
            <trim prefix="security_time = CASE" suffix="END,">
                <foreach collection="demands" item="d">
                    WHEN id = #{d.id} THEN #{d.securityTime}
                </foreach>
            </trim>
            <trim prefix="product_time = CASE" suffix="END,">
                <foreach collection="demands" item="d">
                    WHEN id = #{d.id} THEN #{d.productTime}
                </foreach>
            </trim>
            <trim prefix="total_time = CASE" suffix="END,">
                <foreach collection="demands" item="d">
                    WHEN id = #{d.id} THEN #{d.totalTime}
                </foreach>
            </trim>
        </trim>
        WHERE id IN
        <foreach collection="demands" item="d" open="(" close=")" separator=",">
            #{d.id}
        </foreach>
    </update>

    <update id="batchUpdateStatus">
        update product_demand
        <set>
            <if test="demand.status != null">status = #{demand.status},</if>
            <if test="demand.modifyMan != null">modify_man = #{demand.modifyMan},</if>
            <if test="demand.modifyManId != null">modify_man_id = #{demand.modifyManId},</if>
            <if test="demand.modifyDate != null">modify_date = #{demand.modifyDate},</if>
        </set>
        <where>
            id IN
            <foreach collection="ids" item="id" open="(" separator="," close=")">
                #{id}
            </foreach>
        </where>
    </update>

    <select id="getByName" resultType="com.timevale.forward.dal.entity.ProductDemandDO">
        select *
        from product_demand a
        where a.name = #{name}
          and a.is_deleted = false
    </select>
    <select id="selectByProjectId" resultType="com.timevale.forward.dal.entity.ProductDemandDO">
        select *
        from product_demand
        where id in
              (select product_demand_id
               from project_product_demand
               where project_id = #{projectId}
                 and is_deleted = false)
          and is_deleted = false
    </select>

    <select id="linkProductDemandInCustomDemand" resultType="com.timevale.forward.dal.entity.ProductDemandListDO">
        SELECT a.id,
               a.name,
               a.priority,
               a.owner,
               a.status,
               a.create_man,
               a.create_date,
               b.modify_date
        FROM product_demand a
                 JOIN product_custom_demand b ON a.id = b.product_demand_id AND b.is_deleted = false
        WHERE b.custom_demand_id = #{customDemandId}
    </select>

    <select id="getByOwnerId" resultType="com.timevale.forward.dal.entity.ProductDemandDO">
        select a.id
             , a.name
             , a.priority
             , a.type
             , a.owner
             , a.owner_id
             , a.status
             , a.create_man
             , a.create_man_id
             , a.create_date
             , a.modify_date
             , a.product_line_id
             , `desc`
        from product_demand a
        where a.owner_id = #{ownerId}
          and a.is_deleted = false
        order by a.id desc limit 0,3
    </select>

    <select id="getGroupTree" resultType="com.timevale.forward.dal.entity.ProductDemandGroupFieldDO">
        select
        <if test="selectField!= null and selectField!='' ">
            ${selectField},
        </if>
        count(DISTINCT a.id) as total
        from product_demand a
        left join product_line c on a.product_line_id=c.id
        left join biz_domain d on c.biz_domain_id=d.id
        left join biz_label e on e.biz_id=a.id and e.type = 11 and e.is_deleted = false
        left join label f on f.id = e.label_id and f.is_deleted = false
        <where>
            <if test="condition != null">
                <if test="condition.id != null">and a.id = #{condition.id}</if>
                <if test="condition.name != null and condition.name != ''">and a.name like
                    concat('%',#{condition.name},'%')
                </if>
                <if test="condition.copierId != null and condition.copierId != ''">and e.user_id =
                    #{condition.copierId}
                </if>
                <if test="condition.createDateStart != null and condition.createDateEnd != null">
                    and a.create_date &gt;= #{condition.createDateStart} and a.create_date &lt;=
                    #{condition.createDateEnd}
                </if>
                <if test="condition.modifyDateStart != null and condition.modifyDateEnd != null">
                    and a.modify_date &gt;= #{condition.modifyDateStart} and a.modify_date &lt;=
                    #{condition.modifyDateEnd}
                </if>
                <if test="condition.expectScheduleTimeStart != null and condition.expectScheduleTimeEnd != null">
                    and a.expect_schedule_time &gt;= #{condition.expectScheduleTimeStart} and a.expect_schedule_time
                    &lt;= #{condition.expectScheduleTimeEnd}
                </if>
                <if test="condition.priorities!=null and condition.priorities.size()>0">
                    and a.priority in
                    <foreach collection="condition.priorities" item="item" separator="," open="(" close=")">
                        #{item}
                    </foreach>
                </if>
                <if test="condition.productLineIds!=null and condition.productLineIds.size()>0">
                    and c.id in
                    <foreach collection="condition.productLineIds" item="item" separator="," open="(" close=")">
                        #{item}
                    </foreach>
                </if>
                <if test="condition.bizDomainIds!=null and condition.bizDomainIds.size()>0">
                    and d.id in
                    <foreach collection="condition.bizDomainIds" item="item" separator="," open="(" close=")">
                        #{item}
                    </foreach>
                </if>
                <if test="condition.ownerIds!=null and condition.ownerIds.size()>0">
                    and a.owner_id in
                    <foreach collection="condition.ownerIds" item="item" separator="," open="(" close=")">
                        #{item}
                    </foreach>
                </if>
                <if test="condition.status!=null and condition.status.size()>0">
                    and a.status in
                    <foreach collection="condition.status" item="item" separator="," open="(" close=")">
                        #{item}
                    </foreach>
                </if>
                <if test="condition.filterProductDemandIds!=null and condition.filterProductDemandIds.size()>0">
                    and a.id not in
                    <foreach collection="condition.filterProductDemandIds" item="item" separator="," open="(" close=")">
                        #{item}
                    </foreach>
                </if>
                <if test="condition.inProductDemandIds!=null and condition.inProductDemandIds.size()>0">
                    and a.id in
                    <foreach collection="condition.inProductDemandIds" item="item" separator="," open="(" close=")">
                        #{item}
                    </foreach>
                </if>
                <if test="condition.notInProductDemandIds!=null and condition.notInProductDemandIds.size()>0">
                    and a.id not in
                    <foreach collection="condition.notInProductDemandIds" item="item" separator="," open="(" close=")">
                        #{item}
                    </foreach>
                </if>
                <if test="condition.types!= null and condition.types!='' ">
                    and a.type REGEXP CONCAT('[', #{condition.types},']')
                </if>
                <if test="condition.targetCustomer != null and condition.targetCustomer != ''">
                    and a.target_customer like concat('%',#{condition.targetCustomer},'%')
                </if>
                <if test="condition.customerGrade != null and condition.customerGrade != ''">
                    and a.customer_grade = #{condition.customerGrade}
                </if>
                and a.is_deleted =false
            </if>
            <if test="parentCondition != null">
                <if test="parentCondition.expectScheduleTime != null">
                    and DATE_FORMAT(a.expect_schedule_time, '%Y-%m') = #{parentCondition.expectScheduleTime}
                </if>
                <if test="parentCondition.targetCustomer != null and parentCondition.targetCustomer != ''">
                    and a.target_customer = #{parentCondition.targetCustomer}
                </if>
                <if test="parentCondition.customerGrade != null and parentCondition.customerGrade != ''">
                    and a.customer_grade = #{parentCondition.customerGrade}
                </if>
                <if test="parentCondition.priority!=null">
                    and a.priority = #{parentCondition.priority}
                </if>
                <if test="parentCondition.productLineId!=null">
                    and c.id = #{parentCondition.productLineId}
                </if>
                <if test="parentCondition.bizDomainId!=null">
                    and d.id = #{parentCondition.bizDomainId}
                </if>
                <if test="parentCondition.ownerId!=null">
                    and a.owner_id = #{parentCondition.ownerId}
                </if>
                <if test="parentCondition.status!=null">
                    and a.status = #{parentCondition.status}
                </if>
                <if test="parentCondition.labelCategoryIds!=null">
                    and f.label_category_id in
                    <foreach collection="parentCondition.labelCategoryIds" item="item" separator="," open="(" close=")">
                        #{item}
                    </foreach>
                </if>
                <if test="parentCondition.type!= null">
                    and a.type REGEXP CONCAT('[', #{parentCondition.type},']')
                </if>
            </if>
        </where>
        <if test="groupField!= null and groupField!='' ">
            group by ${groupField}
        </if>
        <if test="orderField!= null and orderField!='' ">
            order by ${orderField}
        </if>
    </select>

    <select id="getGroupList" resultType="com.timevale.forward.dal.entity.ProductDemandListDO">
        select a.id,a.name,a.priority,a.owner,a.owner_id,a.status, a.product_line_id,
        a.create_man,a.create_man_id,a.create_date,a.modify_date, a.type, a.expect_schedule_time,
        c.name productLineName ,d.name bizDomainName, a.ued_time, a.transfer_time, a.back_time,
        a.front_time, a.qa_time, a.ops_time, a.security_time, a.total_time, a.target_customer, a.customer_grade
        <if test="condition.copierId != null and condition.copierId != ''">
            ,e.user_name copier
        </if>
        from product_demand a
        left join product_line c on a.product_line_id=c.id
        left join biz_domain d on c.biz_domain_id=d.id
        <if test="condition.copierId != null and condition.copierId != ''">
            left join person e on a.id=e.main_id and e.type =20 and e.is_deleted =false
        </if>
        <where>
            <if test="condition != null">
                <if test="condition.id != null">and a.id = #{condition.id}</if>
                <if test="condition.name != null and condition.name != ''">and a.name like
                    concat('%',#{condition.name},'%')
                </if>
                <if test="condition.copierId != null and condition.copierId != ''">and e.user_id =
                    #{condition.copierId}
                </if>
                <if test="condition.createDateStart != null and condition.createDateEnd != null">
                    and a.create_date &gt;= #{condition.createDateStart} and a.create_date &lt;=
                    #{condition.createDateEnd}
                </if>
                <if test="condition.modifyDateStart != null and condition.modifyDateEnd != null">
                    and a.modify_date &gt;= #{condition.modifyDateStart} and a.modify_date &lt;=
                    #{condition.modifyDateEnd}
                </if>
                <if test="condition.expectScheduleTimeStart != null and condition.expectScheduleTimeEnd != null">
                    and a.expect_schedule_time &gt;= #{condition.expectScheduleTimeStart} and a.expect_schedule_time
                    &lt;= #{condition.expectScheduleTimeEnd}
                </if>
                <if test="condition.priorities!=null and condition.priorities.size()>0">
                    and a.priority in
                    <foreach collection="condition.priorities" item="item" separator="," open="(" close=")">
                        #{item}
                    </foreach>
                </if>
                <if test="condition.productLineIds!=null and condition.productLineIds.size()>0">
                    and c.id in
                    <foreach collection="condition.productLineIds" item="item" separator="," open="(" close=")">
                        #{item}
                    </foreach>
                </if>
                <if test="condition.bizDomainIds!=null and condition.bizDomainIds.size()>0">
                    and d.id in
                    <foreach collection="condition.bizDomainIds" item="item" separator="," open="(" close=")">
                        #{item}
                    </foreach>
                </if>
                <if test="condition.ownerIds!=null and condition.ownerIds.size()>0">
                    and a.owner_id in
                    <foreach collection="condition.ownerIds" item="item" separator="," open="(" close=")">
                        #{item}
                    </foreach>
                </if>
                <if test="condition.status!=null and condition.status.size()>0">
                    and a.status in
                    <foreach collection="condition.status" item="item" separator="," open="(" close=")">
                        #{item}
                    </foreach>
                </if>
                <if test="condition.filterProductDemandIds!=null and condition.filterProductDemandIds.size()>0">
                    and a.id not in
                    <foreach collection="condition.filterProductDemandIds" item="item" separator="," open="(" close=")">
                        #{item}
                    </foreach>
                </if>
                <if test="condition.inProductDemandIds!=null and condition.inProductDemandIds.size()>0">
                    and a.id in
                    <foreach collection="condition.inProductDemandIds" item="item" separator="," open="(" close=")">
                        #{item}
                    </foreach>
                </if>
                <if test="condition.notInProductDemandIds!=null and condition.notInProductDemandIds.size()>0">
                    and a.id not in
                    <foreach collection="condition.notInProductDemandIds" item="item" separator="," open="(" close=")">
                        #{item}
                    </foreach>
                </if>
                <if test="condition.types!= null and condition.types!='' ">
                    and a.type REGEXP CONCAT('[', #{condition.types},']')
                </if>
                <if test="condition.targetCustomer != null and condition.targetCustomer != ''">
                    and a.target_customer like concat('%',#{condition.targetCustomer},'%')
                </if>
                <if test="condition.customerGrade != null and condition.customerGrade != ''">
                    and a.customer_grade = #{condition.customerGrade}
                </if>
                and a.is_deleted =false
            </if>
            <if test="parentCondition != null">
                <if test="parentCondition.expectScheduleTime != null">
                    and DATE_FORMAT(a.create_date, '%Y-%m') = #{parentCondition.expectScheduleTime}
                </if>
                <if test="parentCondition.targetCustomer != null and parentCondition.targetCustomer != ''">
                    and a.target_customer = #{parentCondition.targetCustomer}
                </if>
                <if test="parentCondition.customerGrade != null and parentCondition.customerGrade != ''">
                    and a.customer_grade = #{parentCondition.customerGrade}
                </if>
                <if test="parentCondition.priority!=null">
                    and a.priority = #{parentCondition.priority}
                </if>
                <if test="parentCondition.productLineId!=null">
                    and c.id = #{parentCondition.productLineId}
                </if>
                <if test="parentCondition.bizDomainId!=null">
                    and d.id = #{parentCondition.bizDomainId}
                </if>
                <if test="parentCondition.ownerId!=null">
                    and a.owner_id = #{parentCondition.ownerId}
                </if>
                <if test="parentCondition.notInOwnerIds!=null and parentCondition.notInOwnerIds.size()>0">
                    and a.owner_id not in
                    <foreach collection="parentCondition.notInOwnerIds" item="item" separator="," open="(" close=")">
                        #{item}
                    </foreach>
                </if>
                <if test="parentCondition.status!=null">
                    and a.status = #{parentCondition.status}
                </if>
                <if test="parentCondition.inProductDemandIds!=null and parentCondition.inProductDemandIds.size()>0">
                    and a.id in
                    <foreach collection="parentCondition.inProductDemandIds" item="item" separator="," open="("
                             close=")">
                        #{item}
                    </foreach>
                </if>
                <if test="parentCondition.notInProductDemandIds!=null and parentCondition.notInProductDemandIds.size()>0">
                    and a.id not in
                    <foreach collection="parentCondition.notInProductDemandIds" item="item" separator="," open="("
                             close=")">
                        #{item}
                    </foreach>
                </if>
                <if test="parentCondition.type!= null">
                    and a.type REGEXP CONCAT('[', #{parentCondition.type},']')
                </if>
            </if>
        </where>
        <if test="orderField!= null and orderField!='' ">
            order by ${orderField}
        </if>
    </select>

    <select id="getSimpleGroupList" resultType="com.timevale.forward.dal.entity.ProductDemandGroupFieldDO">
        select a.id,a.priority,a.owner_id,a.status,c.biz_domain_id, a.product_line_id,
        a.type, a.expect_schedule_time,e.label_id, f.label_category_id, a.target_customer, a.customer_grade
        from product_demand a
        left join product_line c on a.product_line_id=c.id
        left join biz_domain d on c.biz_domain_id=d.id
        left join biz_label e on e.biz_id=a.id and e.type = 11 and e.is_deleted = false
        left join label f on f.id = e.label_id and f.is_deleted = false
        <if test="condition.copierId != null and condition.copierId != ''">
            left join person e on a.id=e.main_id and e.type =20 and e.is_deleted =false
        </if>
        <where>
            <if test="condition != null">
                <if test="condition.id != null">and a.id = #{condition.id}</if>
                <if test="condition.name != null and condition.name != ''">and a.name like
                    concat('%',#{condition.name},'%')
                </if>
                <if test="condition.copierId != null and condition.copierId != ''">and e.user_id =
                    #{condition.copierId}
                </if>
                <if test="condition.createDateStart != null and condition.createDateEnd != null">
                    and a.create_date &gt;= #{condition.createDateStart} and a.create_date &lt;=
                    #{condition.createDateEnd}
                </if>
                <if test="condition.modifyDateStart != null and condition.modifyDateEnd != null">
                    and a.modify_date &gt;= #{condition.modifyDateStart} and a.modify_date &lt;=
                    #{condition.modifyDateEnd}
                </if>
                <if test="condition.expectScheduleTimeStart != null and condition.expectScheduleTimeEnd != null">
                    and a.expect_schedule_time &gt;= #{condition.expectScheduleTimeStart} and a.expect_schedule_time
                    &lt;= #{condition.expectScheduleTimeEnd}
                </if>
                <if test="condition.priorities!=null and condition.priorities.size()>0">
                    and a.priority in
                    <foreach collection="condition.priorities" item="item" separator="," open="(" close=")">
                        #{item}
                    </foreach>
                </if>
                <if test="condition.productLineIds!=null and condition.productLineIds.size()>0">
                    and c.id in
                    <foreach collection="condition.productLineIds" item="item" separator="," open="(" close=")">
                        #{item}
                    </foreach>
                </if>
                <if test="condition.bizDomainIds!=null and condition.bizDomainIds.size()>0">
                    and d.id in
                    <foreach collection="condition.bizDomainIds" item="item" separator="," open="(" close=")">
                        #{item}
                    </foreach>
                </if>
                <if test="condition.ownerIds!=null and condition.ownerIds.size()>0">
                    and a.owner_id in
                    <foreach collection="condition.ownerIds" item="item" separator="," open="(" close=")">
                        #{item}
                    </foreach>
                </if>
                <if test="condition.status!=null and condition.status.size()>0">
                    and a.status in
                    <foreach collection="condition.status" item="item" separator="," open="(" close=")">
                        #{item}
                    </foreach>
                </if>
                <if test="condition.filterProductDemandIds!=null and condition.filterProductDemandIds.size()>0">
                    and a.id not in
                    <foreach collection="condition.filterProductDemandIds" item="item" separator="," open="(" close=")">
                        #{item}
                    </foreach>
                </if>
                <if test="condition.inProductDemandIds!=null and condition.inProductDemandIds.size()>0">
                    and a.id in
                    <foreach collection="condition.inProductDemandIds" item="item" separator="," open="(" close=")">
                        #{item}
                    </foreach>
                </if>
                <if test="condition.notInProductDemandIds!=null and condition.notInProductDemandIds.size()>0">
                    and a.id not in
                    <foreach collection="condition.notInProductDemandIds" item="item" separator="," open="(" close=")">
                        #{item}
                    </foreach>
                </if>
                <if test="condition.types!= null and condition.types!='' ">
                    and a.type REGEXP CONCAT('[', #{condition.types},']')
                </if>
                <if test="condition.targetCustomer != null and condition.targetCustomer != ''">
                    and a.target_customer like concat('%',#{condition.targetCustomer},'%')
                </if>
                <if test="condition.customerGrade != null and condition.customerGrade != ''">
                    and a.customer_grade = #{condition.customerGrade}
                </if>
                and a.is_deleted =false
            </if>
            <if test="parentCondition != null">
                <if test="parentCondition.expectScheduleTime != null">
                    and DATE_FORMAT(a.create_date, '%Y-%m') = #{parentCondition.expectScheduleTime}
                </if>
                <if test="parentCondition.targetCustomer != null and parentCondition.targetCustomer != ''">
                    and a.target_customer = #{parentCondition.targetCustomer}
                </if>
                <if test="parentCondition.customerGrade != null and parentCondition.customerGrade != ''">
                    and a.customer_grade = #{parentCondition.customerGrade}
                </if>
                <if test="parentCondition.priority!=null">
                    and a.priority = #{parentCondition.priority}
                </if>
                <if test="parentCondition.productLineId!=null">
                    and c.id = #{parentCondition.productLineId}
                </if>
                <if test="parentCondition.bizDomainId!=null">
                    and d.id = #{parentCondition.bizDomainId}
                </if>
                <if test="parentCondition.ownerId!=null">
                    and a.owner_id = #{parentCondition.ownerId}
                </if>
                <if test="parentCondition.ownerIds!=null and parentCondition.ownerIds.size()>0">
                    and a.owner_id not in
                    <foreach collection="parentCondition.ownerIds" item="item" separator="," open="(" close=")">
                        #{item}
                    </foreach>
                </if>
                <if test="parentCondition.status!=null">
                    and a.status = #{parentCondition.status}
                </if>
                <if test="parentCondition.inProductDemandIds!=null and parentCondition.inProductDemandIds.size()>0">
                    and a.id in
                    <foreach collection="parentCondition.inProductDemandIds" item="item" separator="," open="("
                             close=")">
                        #{item}
                    </foreach>
                </if>
                <if test="parentCondition.type!= null">
                    and a.type REGEXP CONCAT('[', #{parentCondition.type},']')
                </if>
                <if test="parentCondition.labelCategoryIds!=null and parentCondition.labelCategoryIds.size()>0">
                    and f.label_category_id in
                    <foreach collection="parentCondition.labelCategoryIds" item="item" separator="," open="(" close=")">
                        #{item}
                    </foreach>
                </if>
            </if>
        </where>
        <if test="orderField!= null and orderField!='' ">
            order by ${orderField}
        </if>
    </select>

    <select id="getSimpleGroupCount" resultType="long">
        select count(distinct a.id) as total
        from product_demand a
        left join product_line c on a.product_line_id=c.id
        left join biz_domain d on c.biz_domain_id=d.id
        left join biz_label e on e.biz_id=a.id and e.type = 11 and e.is_deleted = false
        left join label f on f.id = e.label_id and f.is_deleted = false
        <if test="condition.copierId != null and condition.copierId != ''">
            left join person e on a.id=e.main_id and e.type =20 and e.is_deleted =false
        </if>
        <where>
            <if test="condition != null">
                <if test="condition.id != null">and a.id = #{condition.id}</if>
                <if test="condition.name != null and condition.name != ''">and a.name like
                    concat('%',#{condition.name},'%')
                </if>
                <if test="condition.copierId != null and condition.copierId != ''">and e.user_id =
                    #{condition.copierId}
                </if>
                <if test="condition.createDateStart != null and condition.createDateEnd != null">
                    and a.create_date &gt;= #{condition.createDateStart} and a.create_date &lt;=
                    #{condition.createDateEnd}
                </if>
                <if test="condition.modifyDateStart != null and condition.modifyDateEnd != null">
                    and a.modify_date &gt;= #{condition.modifyDateStart} and a.modify_date &lt;=
                    #{condition.modifyDateEnd}
                </if>
                <if test="condition.expectScheduleTimeStart != null and condition.expectScheduleTimeEnd != null">
                    and a.expect_schedule_time &gt;= #{condition.expectScheduleTimeStart} and a.expect_schedule_time
                    &lt;= #{condition.expectScheduleTimeEnd}
                </if>
                <if test="condition.priorities!=null and condition.priorities.size()>0">
                    and a.priority in
                    <foreach collection="condition.priorities" item="item" separator="," open="(" close=")">
                        #{item}
                    </foreach>
                </if>
                <if test="condition.productLineIds!=null and condition.productLineIds.size()>0">
                    and c.id in
                    <foreach collection="condition.productLineIds" item="item" separator="," open="(" close=")">
                        #{item}
                    </foreach>
                </if>
                <if test="condition.bizDomainIds!=null and condition.bizDomainIds.size()>0">
                    and d.id in
                    <foreach collection="condition.bizDomainIds" item="item" separator="," open="(" close=")">
                        #{item}
                    </foreach>
                </if>
                <if test="condition.ownerIds!=null and condition.ownerIds.size()>0">
                    and a.owner_id in
                    <foreach collection="condition.ownerIds" item="item" separator="," open="(" close=")">
                        #{item}
                    </foreach>
                </if>
                <if test="condition.status!=null and condition.status.size()>0">
                    and a.status in
                    <foreach collection="condition.status" item="item" separator="," open="(" close=")">
                        #{item}
                    </foreach>
                </if>
                <if test="condition.filterProductDemandIds!=null and condition.filterProductDemandIds.size()>0">
                    and a.id not in
                    <foreach collection="condition.filterProductDemandIds" item="item" separator="," open="(" close=")">
                        #{item}
                    </foreach>
                </if>
                <if test="condition.inProductDemandIds!=null and condition.inProductDemandIds.size()>0">
                    and a.id in
                    <foreach collection="condition.inProductDemandIds" item="item" separator="," open="(" close=")">
                        #{item}
                    </foreach>
                </if>
                <if test="condition.notInProductDemandIds!=null and condition.notInProductDemandIds.size()>0">
                    and a.id not in
                    <foreach collection="condition.notInProductDemandIds" item="item" separator="," open="(" close=")">
                        #{item}
                    </foreach>
                </if>
                <if test="condition.types!= null and condition.types!='' ">
                    and a.type REGEXP CONCAT('[', #{condition.types},']')
                </if>
                <if test="condition.targetCustomer != null and condition.targetCustomer != ''">
                    and a.target_customer like concat('%',#{condition.targetCustomer},'%')
                </if>
                <if test="condition.customerGrade != null and condition.customerGrade != ''">
                    and a.customer_grade = #{condition.customerGrade}
                </if>
                and a.is_deleted =false
            </if>
            <if test="parentCondition != null">
                <if test="parentCondition.expectScheduleTime != null">
                    and DATE_FORMAT(a.create_date, '%Y-%m') = #{parentCondition.expectScheduleTime}
                </if>
                <if test="parentCondition.targetCustomer != null and parentCondition.targetCustomer != ''">
                    and a.target_customer = #{parentCondition.targetCustomer}
                </if>
                <if test="parentCondition.customerGrade != null and parentCondition.customerGrade != ''">
                    and a.customer_grade = #{parentCondition.customerGrade}
                </if>
                <if test="parentCondition.priority!=null">
                    and a.priority = #{parentCondition.priority}
                </if>
                <if test="parentCondition.productLineId!=null">
                    and c.id = #{parentCondition.productLineId}
                </if>
                <if test="parentCondition.bizDomainId!=null">
                    and d.id = #{parentCondition.bizDomainId}
                </if>
                <if test="parentCondition.ownerId!=null">
                    and a.owner_id = #{parentCondition.ownerId}
                </if>
                <if test="parentCondition.ownerIds!=null and parentCondition.ownerIds.size()>0">
                    and a.owner_id not in
                    <foreach collection="parentCondition.ownerIds" item="item" separator="," open="(" close=")">
                        #{item}
                    </foreach>
                </if>
                <if test="parentCondition.status!=null">
                    and a.status = #{parentCondition.status}
                </if>
                <if test="parentCondition.inProductDemandIds!=null and parentCondition.inProductDemandIds.size()>0">
                    and a.id in
                    <foreach collection="parentCondition.inProductDemandIds" item="item" separator="," open="("
                             close=")">
                        #{item}
                    </foreach>
                </if>
                <if test="parentCondition.type!= null">
                    and a.type REGEXP CONCAT('[', #{parentCondition.type},']')
                </if>
                <if test="parentCondition.labelCategoryIds!=null and parentCondition.labelCategoryIds.size()>0">
                    and f.label_category_id in
                    <foreach collection="parentCondition.labelCategoryIds" item="item" separator="," open="(" close=")">
                        #{item}
                    </foreach>
                </if>
                <if test="parentCondition.notInProductDemandIds!=null and parentCondition.notInProductDemandIds.size()>0">
                    and a.id not in
                    <foreach collection="parentCondition.notInProductDemandIds" item="item" separator="," open="("
                             close=")">
                        #{item}
                    </foreach>
                </if>
            </if>
        </where>
        <if test="orderField!= null and orderField!='' ">
            order by ${orderField}
        </if>
    </select>

    <select id="listProductDemandOwners" resultType="com.timevale.forward.dal.entity.ProductDemandOwnerDO">
        SELECT id,owner,owner_id,product_demand_id,resource_type
        FROM product_demand_owner
        WHERE is_deleted = 0
        <if test="productDemandIds != null and !productDemandIds.isEmpty()">
            AND product_demand_id IN
            <foreach collection="productDemandIds" item="demandId" open="(" close=")" separator=",">
                #{demandId}
            </foreach>
        </if>
        <if test="productDemandIds == null or productDemandIds.isEmpty()">
            AND 1 = 0
        </if>
    </select>
    <select id="listProductDemandOwnersByGroupId"
            resultType="com.timevale.forward.dal.entity.ProductDemandOwnerDO">
        select pdo.id, pdo.product_demand_id, pdo.owner_id, pdo.owner, pdo.resource_type
        from product_demand_group_item pdgi inner join product_demand_owner pdo
        on pdgi.product_demand_id = pdo.product_demand_id
        where pdo.is_deleted = 0
        and pdgi.product_demand_group_id = ${productDemandGroupId};
    </select>

    <select id="listProductDemandOwnersByIds"
            resultType="com.timevale.forward.dal.entity.ProductDemandOwnerDO">
        select pdo.id, pdo.product_demand_id, pdo.owner_id, pdo.owner, pdo.resource_type
        from product_demand_owner pdo
        where pdo.is_deleted = 0
        <if test="productDemandIds != null and !productDemandIds.isEmpty()">
            AND product_demand_id IN
            <foreach collection="productDemandIds" item="demandId" open="(" close=")" separator=",">
                #{demandId}
            </foreach>
        </if>
    </select>

    <select id="simpleList" resultType="com.timevale.forward.dal.entity.ProductDemandListDO">
        select a.`id`, a.`name`, a.`desc`, c.`name` as productLineName, a.`owner`, a.`owner_id`
        from product_demand as a
        left join product_line c on a.product_line_id=c.id
        <if test="bizDomainIds != null and bizDomainIds.size() > 0">
            left join biz_domain d on c.biz_domain_id=d.id
        </if>
        <where>
            a.is_deleted =false
            <if test="id != null">and a.id = #{id}</if>
            <if test="name != null and name != ''">and a.name like concat('%',#{name},'%')</if>
            <if test="ids!=null and ids.size()>0">
                and a.id in
                <foreach collection="ids" item="item" separator="," open="(" close=")">
                    #{item}
                </foreach>
            </if>
            <if test="bizDomainIds!=null and bizDomainIds.size()>0">
                and d.id in
                <foreach collection="bizDomainIds" item="item" separator="," open="(" close=")">
                    #{item}
                </foreach>
            </if>
            <if test="ownerName != null and ownerName !=''">
                and a.owner like concat('%',#{ownerName},'%')
            </if>
            <if test="productLineName != null and productLineName !=''">
                and c.name like concat('%',#{productLineName},'%')
            </if>
            <if test="ownerIds!=null and ownerIds.size()>0">
                and a.owner_id in
                <foreach collection="ownerIds" item="item" separator="," open="(" close=")">
                    #{item}
                </foreach>
            </if>
            <if test="productLineIds!=null and productLineIds.size()>0">
                and c.id in
                <foreach collection="productLineIds" item="item" separator="," open="(" close=")">
                    #{item}
                </foreach>
            </if>
        </where>
        order by a.id
    </select>
</mapper>
