<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<!--你接口的包名是com.abc.dao,接口名是NameMapper.java，那么你的mapper.xml的namespace应该是com.abc.dao.NameMapper-->
<mapper namespace="com.timevale.forward.dal.dao.BizDemandMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO info_forward.biz_demand
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="name != null">name,</if>
            <if test="productLineId != null">product_line_id,</if>
            <if test="dataIndicators != null">data_indicators,</if>
            <if test="targetCustomer != null">target_customer,</if>
            <if test="createCustomer != null">create_customer,</if>
            <if test="deptId != null">dept_id,</if>
            <if test="desc != null">`desc`,</if>
            <if test="status != null">`status`,</if>
            <if test="priority != null">priority,</if>
            <if test="receiveMan != null">receive_man,</if>
            <if test="receiveManId != null">receive_man_id,</if>
            <if test="submitManId != null">submit_man_id,</if>
            <if test="submitMan != null">submit_man,</if>
            <if test="createManId != null">create_man_id,</if>
            <if test="createMan != null">create_man,</if>
            <if test="customerDevDemand != null">customer_dev_demand,</if>
            <if test="customerDevType != null">customer_dev_type,</if>
            <if test="customerDevSaleContract != null">customer_dev_sale_contract,</if>
            <if test="customerDevProjectName != null">customer_dev_project_name,</if>
            <if test="sourceId != null">source_id,</if>
            <if test="bizId != null">biz_id,</if>
            <if test="bizName != null">biz_name,</if>
            <if test="hopeReleaseDay != null">hope_release_day,</if>
            <if test="uedTime != null">ued_time,</if>
            <if test="frontTime != null">front_time,</if>
            <if test="backTime != null">back_time,</if>
            <if test="qaTime != null">qa_time,</if>
            <if test="transferTime != null">transfer_time,</if>
            <if test="productTime != null">product_time,</if>
            <if test="totalTime != null">total_time,</if>
            <if test="srExpert != null">sr_expert,</if>
            <if test="srExpertId != null">sr_expert_id,</if>
            <if test="affectCustomerOrder != null">affect_customer_order,</if>
            <if test="stuckOrderInstructions  != null">stuck_order_instructions,</if>
            <if test="customerGrade != null">customer_grade,</if>
        </trim>
        VALUES
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="name != null">#{name},</if>
            <if test="productLineId != null">#{productLineId},</if>
            <if test="dataIndicators != null">#{dataIndicators},</if>
            <if test="targetCustomer != null">#{targetCustomer},</if>
            <if test="createCustomer != null">#{createCustomer},</if>
            <if test="deptId != null">#{deptId},</if>
            <if test="desc != null">#{desc},</if>
            <if test="status != null">#{status},</if>
            <if test="priority != null">#{priority},</if>
            <if test="receiveMan != null">#{receiveMan},</if>
            <if test="receiveManId != null">#{receiveManId},</if>
            <if test="submitManId != null">#{submitManId},</if>
            <if test="submitMan != null">#{submitMan},</if>
            <if test="createManId != null">#{createManId},</if>
            <if test="createMan != null">#{createMan},</if>
            <if test="customerDevDemand != null">#{customerDevDemand},</if>
            <if test="customerDevType != null">#{customerDevType},</if>
            <if test="customerDevSaleContract != null">#{customerDevSaleContract},</if>
            <if test="customerDevProjectName != null">#{customerDevProjectName},</if>
            <if test="sourceId != null">#{sourceId},</if>
            <if test="bizId != null">#{bizId},</if>
            <if test="bizName != null">#{bizName},</if>
            <if test="hopeReleaseDay != null">#{hopeReleaseDay},</if>
            <if test="uedTime != null">#{uedTime},</if>
            <if test="frontTime != null">#{frontTime},</if>
            <if test="backTime != null">#{backTime},</if>
            <if test="qaTime != null">#{qaTime},</if>
            <if test="transferTime != null">#{transferTime},</if>
            <if test="productTime != null">#{productTime},</if>
            <if test="totalTime != null">#{totalTime},</if>
            <if test="srExpert != null">#{srExpert},</if>
            <if test="srExpertId != null">#{srExpertId},</if>
            <if test="affectCustomerOrder != null">#{affectCustomerOrder},</if>
            <if test="stuckOrderInstructions != null">#{stuckOrderInstructions},</if>
            <if test="customerGrade != null">#{customerGrade},</if>
        </trim>
    </insert>

    <update id="update">
        update biz_demand
        <set>
            <if test="name!=null and name!='' ">
                name = #{name},
            </if>
            <if test="productLineId!=null">
                product_line_id = #{productLineId},
            </if>
            <if test="dataIndicators!=null">
                data_indicators = #{dataIndicators},
            </if>
            <if test="targetCustomer!=null and targetCustomer!='' ">
                target_customer = #{targetCustomer},
            </if>
            <if test="createCustomer!=null">
                create_customer = #{createCustomer},
            </if>
            <if test="deptId!=null">
                dept_id = #{deptId},
            </if>
            <if test="desc!=null">
                `desc` = #{desc},
            </if>
            <if test="status!=null">
                status = #{status},
            </if>
            <if test="priority!=null">
                priority = #{priority},
            </if>
            <if test="receiveMan!=null">
                receive_man = #{receiveMan},
            </if>
            <if test="receiveManId!=null">
                receive_man_id = #{receiveManId},
            </if>
            <if test="reason!=null">
                reason = #{reason},
            </if>
            <if test="submitMan != null and submitMan != ''">
                submit_man = #{submitMan},
            </if>
            <if test="submitManId != null and submitManId != ''">
                submit_man_id = #{submitManId},
            </if>
            <if test="solvePlan != null">
                solve_plan = #{solvePlan},
            </if>
            <if test="rejectReason != null">
                reject_reason = #{rejectReason},
            </if>
            <if test="planReleaseDate!=null">
                plan_release_date = #{planReleaseDate},
            </if>
            <if test="projectEndDate != null">
                project_end_date = #{projectEndDate},
            </if>
            <if test="customerDevDemand != null">
                customer_dev_demand = #{customerDevDemand},
            </if>
            <if test="customerDevType != null">
                customer_dev_type = #{customerDevType},
            </if>
            <if test="customerDevSaleContract != null and customerDevSaleContract != ''">
                customer_dev_sale_contract = #{customerDevSaleContract},
            </if>
            <if test="customerDevProjectName != null and customerDevProjectName != ''">
                customer_dev_project_name = #{customerDevProjectName},
            </if>
            <if test="sourceId != null and sourceId != ''">
                source_id = #{sourceId},
            </if>
            <if test="hopeReleaseDay != null">
                hope_release_day = #{hopeReleaseDay},
            </if>
            <if test="productSolution != null and productSolution != ''">
                product_solution = #{productSolution},
            </if>
            <if test="srExpert != null and srExpert != ''">
                sr_expert = #{srExpert},
            </if>
            <if test="srExpertId != null and srExpertId != ''">
                sr_expert_id = #{srExpertId},
            </if>
            <if test="affectCustomerOrder != null">
                affect_customer_order = #{affectCustomerOrder},
            </if>
            <if test="stuckOrderInstructions != null">
               stuck_order_instructions = #{stuckOrderInstructions},
            </if>
            <if test="customerGrade != null and customerGrade!=''">
                customer_grade = #{customerGrade},
            </if>
            <if test="true">
                modify_man_id = #{modifyManId},
                modify_man = #{modifyMan},
            </if>
        </set>
        where id = #{id}
    </update>

    <update id="fullUpdate">
        update biz_demand
        <set>
            name = #{name},
            product_line_id = #{productLineId},
            data_indicators = #{dataIndicators},
            target_customer = #{targetCustomer},
            create_customer = #{createCustomer},
            dept_id = #{deptId},
            `desc` = #{desc},
            `status` = #{status},
            priority = #{priority},
            receive_man = #{receiveMan},
            receive_man_id = #{receiveManId},
            reason = #{reason},
            submit_man = #{submitMan},
            submit_man_id = #{submitManId},
            solve_plan = #{solvePlan},
            reject_reason = #{rejectReason},
            plan_release_date = #{planReleaseDate},
            project_end_date = #{projectEndDate},
            source_id = #{sourceId},
            customer_dev_demand = #{customerDevDemand},
            customer_dev_type = #{customerDevType},
            customer_dev_sale_contract = #{customerDevSaleContract},
            customer_dev_project_name = #{customerDevProjectName},
            ued_time = #{uedTime},
            back_time = #{backTime},
            front_time = #{frontTime},
            qa_time = #{qaTime},
            transfer_time = #{transferTime},
            product_time = #{productTime},
            total_time = #{totalTime},
            modify_man_id = #{modifyManId},
            modify_man = #{modifyMan},
            product_solution = #{productSolution},
            hope_release_day = #{hopeReleaseDay},
            sr_expert = #{srExpert},
            sr_expert_id = #{srExpertId},
            affect_customer_order = #{affectCustomerOrder},
            stuck_order_instructions = #{stuckOrderInstructions},
            customer_grade = #{customerGrade},
        </set>
        where id = #{id}
    </update>

    <select id="get" resultType="com.timevale.forward.dal.entity.BizDemandDO">
        SELECT *
        FROM biz_demand
        WHERE id = #{id}
          AND is_deleted = false
    </select>

    <select id="selectByName" resultType="com.timevale.forward.dal.entity.BizDemandDO">
        select id, name
        from biz_demand
        where name = #{name}
          and is_deleted = false
    </select>

    <select id="selectList" resultType="com.timevale.forward.dal.entity.BizDemandListDO">
        select bd.*,
        pl.id productLineId, pl.name productLineName,
        b.id bizDomainId , b.name bizDomainName
        from biz_demand bd
        left join product_line pl on pl.id = bd.product_line_id
        left join biz_domain b on pl.biz_domain_id = b.id
        <where>
            <if test="id!=null">
                and bd.id = #{id}
            </if>
            <if test="containIds!=null and containIds.size()>0">
                and bd.id in
                <foreach collection="containIds" item="item" separator="," open="(" close=")">
                    #{item}
                </foreach>
            </if>
            <if test="exclusiveIds!=null and exclusiveIds.size()>0">
                and bd.id not in
                <foreach collection="exclusiveIds" item="item" separator="," open="(" close=")">
                    #{item}
                </foreach>
            </if>
            <if test="name!=null and name!='' ">
                and bd.name like concat('%',#{name},'%')
            </if>
            <if test="desc!=null and desc!='' ">
                and bd.`desc` like concat('%',#{desc},'%')
            </if>
            <if test="targetCustomer!=null and targetCustomer!='' ">
                and bd.target_customer like concat('%',#{targetCustomer},'%')
            </if>
            <if test="priorityList!=null and priorityList.size()>0">
                and bd.priority in
                <foreach collection="priorityList" item="item" separator="," open="(" close=")">
                    #{item}
                </foreach>
            </if>
            <if test="bizDomainIdList!=null and bizDomainIdList.size()>0">
                and pl.biz_domain_id in
                <foreach collection="bizDomainIdList" item="item" separator="," open="(" close=")">
                    #{item}
                </foreach>
            </if>
            <if test="productLineIdList!=null and productLineIdList.size()>0">
                and bd.product_line_id in
                <foreach collection="productLineIdList" item="item" separator="," open="(" close=")">
                    #{item}
                </foreach>
            </if>
            <if test="planReleaseDateList!=null and planReleaseDateList.size()>0">
                and bd.plan_release_date in
                <foreach collection="planReleaseDateList" item="item" separator="," open="(" close=")">
                    #{item}
                </foreach>
            </if>
            <if test="statusList!=null and statusList.size()>0">
                and bd.status in
                <foreach collection="statusList" item="item" separator="," open="(" close=")">
                    #{item}
                </foreach>
            </if>
            <if test="submitManIdList!=null and submitManIdList.size()>0">
                and bd.submit_man_id in
                <foreach collection="submitManIdList" item="item" separator="," open="(" close=")">
                    #{item}
                </foreach>
            </if>
            <if test="receiveManIdList!=null and receiveManIdList.size()>0">
                and bd.receive_man_id in
                <foreach collection="receiveManIdList" item="item" separator="," open="(" close=")">
                    #{item}
                </foreach>
            </if>
            <if test="deptIdList!=null and deptIdList.size()>0">
                and bd.dept_id in
                <foreach collection="deptIdList" item="item" separator="," open="(" close=")">
                    #{item}
                </foreach>
            </if>
            <if test="bizDemandIds!=null and bizDemandIds.size()>0">
                and bd.id not in
                <foreach collection="bizDemandIds" item="item" separator="," open="(" close=")">
                    #{item}
                </foreach>
            </if>
            <if test="copier!=null and copier!='' ">
                and bd.id IN (
                SELECT p.main_id
                FROM person p
                WHERE p.user_id = #{copier}
                AND p.type = 30
                AND p.is_deleted = false
                )
            </if>
            <if test="createDateStart!=null and createDateEnd!=null">
                and bd.create_date &gt;= #{createDateStart}
                and bd.create_date &lt;= #{createDateEnd}
            </if>
            <if test="projectEndDateStart != null and projectEndDateEnd != null">
                and bd.project_end_date is not null
                and bd.project_end_date &gt;= #{projectEndDateStart}
                and bd.project_end_date &lt;= #{projectEndDateEnd}
            </if>
            <if test="sourceId != null and sourceId != ''">
                and bd.source_id = #{sourceId}
            </if>
            <if test="customerDevDemand != null">
                and bd.customer_dev_demand = #{customerDevDemand}
            </if>
            <if test="hopeReleaseDayStart != null">
                and bd.hope_release_day >= date(#{hopeReleaseDayStart})
            </if>
            <if test="hopeReleaseDayEnd != null">
                and bd.hope_release_day &lt;= date(#{hopeReleaseDayEnd})
            </if>
            <if test="productSolution != null and productSolution != ''">
                and bd.product_solution like concat('%', #{productSolution}, '%')
            </if>
            <if test="srExpertIds != null and srExpertIds.size() > 0">
                AND bd.sr_expert_id IN
                <foreach collection="srExpertIds" item="item" separator="," open="(" close=")">
                    #{item}
                </foreach>
            </if>
            <if test="affectCustomerOrder != null">
                and bd.affect_customer_order = #{affectCustomerOrder}
            </if>
            <if test="stuckOrderInstructions != null and stuckOrderInstructions != ''">
                and bd.stuck_order_instructions like concat('%', #{stuckOrderInstructions}, '%')
            </if>
            <if test="customerGrade != null and customerGrade!=''">
                and bd.customer_grade = #{customerGrade}
            </if>
            and bd.is_deleted = false
        </where>
    </select>

    <select id="linkBizDemandList" resultType="com.timevale.forward.dal.entity.BizDemandListDO">
        select a.id, a.name,a.priority,a.dept_id,a.modify_date,a.create_man,a.create_man_id, a.submit_man, a.submit_man_id, a.target_customer, a.customer_grade
        from biz_demand a
        left join product_biz_demand b on a.id=b.biz_demand_id and b.is_deleted=false
        where b.product_demand_id = #{productDemandId} and   a.is_deleted = false
    </select>

    <update id="updateByIds">
        update biz_demand
        <set>
            <if test="retainModifyDate">modify_date = modify_date,</if>
            status =#{status}
        </set>
        <where>
            and id IN
            <foreach item="item" index="index" collection="ids" open="(" separator="," close=")">
                #{item}
            </foreach>
        </where>
    </update>

    <update id="updateCreateMan">
        UPDATE biz_demand
        SET create_man = #{createMan}, create_man_id = #{createManId}
        WHERE id IN
        <foreach collection="idList" item="item" separator="," open="(" close=")">
            #{item}
        </foreach>
    </update>

    <update id="updateSubmitMan">
        UPDATE biz_demand
        SET submit_man = #{submitMan}, submit_man_id = #{submitManId}, dept_id = #{deptId}
        WHERE id IN
        <foreach collection="idList" item="item" separator="," open="(" close=")">
            #{item}
        </foreach>
    </update>

    <update id="updateReceiveMan">
        UPDATE biz_demand
        SET receive_man = #{receiveMan}, receive_man_id = #{receiveManId}
        WHERE id IN
        <foreach collection="idList" item="item" separator="," open="(" close=")">
            #{item}
        </foreach>
    </update>

    <update id="updateDate">
        UPDATE biz_demand
        <set>
            <if test="projectEndDate != null">project_end_date = #{projectEndDate},</if>
            <if test="planReleaseDate != null">plan_release_date = #{planReleaseDate},</if>
            <if test="true">modify_date = modify_date,</if>
            <if test="true">modify_man = modify_man,</if>
            <if test="true">modify_man_id = modify_man_id,</if>
        </set>
        <where>
            id = #{id}
        </where>
    </update>

    <update id="updateCanNull">
        UPDATE biz_demand
        <set>
            project_end_date = #{projectEndDate},
            plan_release_date = #{planReleaseDate},
            <if test="modifyMan != null">modify_man = #{modifyMan},</if>
            <if test="modifyManId != null">modify_man_id = #{modifyManId},</if>
        </set>
        <where>
            id = #{id}
        </where>
    </update>
    <update id="updateConditional">
        update biz_demand
        <set>
            <if test="status != null">
                status = #{status},
            </if>
            <if test="projectEndDate != null">
                project_end_date = #{projectEndDate},
            </if>
            <if test="customerDevDemand != null">
                customer_dev_demand = #{customerDevDemand},
            </if>
            <if test="projectEndDateNull">
                project_end_date = null,
            </if>
        </set>
        <where>
            id in
            <foreach collection="ids" item="id" open="(" separator="," close=")">
                #{id}
            </foreach>
        </where>
    </update>

    <update id="updateReason">
        UPDATE biz_demand
        SET reason = #{reason},
            modify_man = #{modifyMan},
            modify_man_id = #{modifyManId}
        WHERE id = #{id}
    </update>

    <select id="getByIds" resultType="com.timevale.forward.dal.entity.BizDemandDO">
        select *
        from biz_demand
        <where>
             id in
            <foreach collection="ids" item="item" separator="," open="(" close=")">
                #{item}
            </foreach>
              and is_deleted = false
        </where>
    </select>

    <select id="selectByStatus" resultType="com.timevale.forward.dal.entity.BizDemandDO">
        select id, name, product_line_id, data_indicators, target_customer, create_customer, dept_id, `desc`, status, priority,
        receive_man, receive_man_id, submit_man, submit_man_id, plan_release_date, reason,
        is_deleted, create_man_id, create_man, create_date, modify_man_id, modify_man, modify_date, source_id
        from biz_demand
        <where>
            status in
            <foreach collection="status" item="item" separator="," open="(" close=")">
                #{item}
            </foreach>
            and is_deleted = false
        </where>
    </select>

    <select id="selectAll" resultType="com.timevale.forward.dal.entity.BizDemandDO">
        SELECT *
        FROM biz_demand;
    </select>

    <select id="getSimpleBizDemands" resultType="com.timevale.forward.dal.entity.BizDemandDO">
        select id, name, product_line_id, data_indicators, target_customer, create_customer, dept_id, `desc`, status,
        priority, receive_man, receive_man_id, submit_man, submit_man_id, plan_release_date, reason, is_deleted,
        create_man_id, create_man, create_date, modify_man_id, modify_man, modify_date, source_id, ued_time,
        back_time, front_time, qa_time, transfer_time, total_time, customer_dev_demand
        from biz_demand
        <where>
            <if test="ids!=null and ids.size()>0">
                and id in
                <foreach collection="ids" item="item" separator="," open="(" close=")">
                    #{item}
                </foreach>
            </if>
            <if test="status!=null and status.size()>0">
                and `status` in
                <foreach collection="status" item="item" separator="," open="(" close=")">
                    #{item}
                </foreach>
            </if>
            <if test="sourceId != null and sourceId!=''">and source_id = #{sourceId}</if>
            and is_deleted = false
        </where>
    </select>

    <select id="selectByCustomId" resultType="com.timevale.forward.dal.entity.BizDemandListDO">
          select b.id, b.name, b.status, b.receive_man,  b.receive_man_id,
               b.submit_man, b.submit_man_id, b.plan_release_date, b.project_end_date,
               b.create_date
        from biz_demand b
          left join biz_demand_custom bc on b.id = bc.biz_demand_id where bc.custom_id = #{customId} and bc.is_deleted=0 and b.is_deleted=0  order by b.create_date desc
    </select>
    <select id="selectBySourceIds" resultType="com.timevale.forward.dal.entity.BizDemandDO">
        select * from biz_demand where source_id in
        <foreach collection="sourceIds" item="sourceId" open="(" separator="," close=")">
            #{sourceId}
        </foreach>
        and is_deleted = false
    </select>

    <select id="getByProjectId" resultType="com.timevale.forward.dal.entity.BizDemandDO">
        SELECT *
        FROM biz_demand bd
        INNER JOIN project_biz_demand pbd on bd.id = pbd.biz_demand_id AND pbd.is_deleted = false
        WHERE bd.is_deleted = false AND pbd.project_id = #{projectId}
    </select>

    <select id="getGroupTree" resultType="com.timevale.forward.dal.entity.BizDemandGroupFieldDO">
        select
        <if test="selectField!= null and selectField!='' ">
            ${selectField},
        </if>
        count(DISTINCT bd.id) as total
        from biz_demand bd
        left join product_line pl on pl.id = bd.product_line_id
        left join biz_domain b on pl.biz_domain_id = b.id
        left join biz_label e on e.biz_id=bd.id and e.type = 10
        left join label f on f.id=e.label_id
        <where>
            <if test="condition != null">
                <if test="condition.id!=null">
                    and bd.id = #{condition.id}
                </if>
                <if test="condition.containIds!=null and condition.containIds.size()>0">
                    and bd.id in
                    <foreach collection="condition.containIds" item="item" separator="," open="(" close=")">
                        #{item}
                    </foreach>
                </if>
                <if test="condition.exclusiveIds!=null and condition.exclusiveIds.size()>0">
                    and bd.id not in
                    <foreach collection="condition.exclusiveIds" item="item" separator="," open="(" close=")">
                        #{item}
                    </foreach>
                </if>
                <if test="condition.name!=null and condition.name!='' ">
                    and bd.name like concat('%',#{condition.name},'%')
                </if>
                <if test="condition.desc!=null and condition.desc!='' ">
                    and bd.`desc` like concat('%',#{condition.desc},'%')
                </if>
                <if test="condition.targetCustomer!=null and condition.targetCustomer!='' ">
                    and bd.target_customer like concat('%',#{condition.targetCustomer},'%')
                </if>
                <if test="condition.priorityList!=null and condition.priorityList.size()>0">
                    and bd.priority in
                    <foreach collection="condition.priorityList" item="item" separator="," open="(" close=")">
                        #{item}
                    </foreach>
                </if>
                <if test="condition.bizDomainIdList!=null and condition.bizDomainIdList.size()>0">
                    and pl.biz_domain_id in
                    <foreach collection="condition.bizDomainIdList" item="item" separator="," open="(" close=")">
                        #{item}
                    </foreach>
                </if>
                <if test="condition.productLineIdList!=null and condition.productLineIdList.size()>0">
                    and bd.product_line_id in
                    <foreach collection="condition.productLineIdList" item="item" separator="," open="(" close=")">
                        #{item}
                    </foreach>
                </if>
                <if test="condition.planReleaseDateList!=null and condition.planReleaseDateList.size()>0">
                    and bd.plan_release_date in
                    <foreach collection="condition.planReleaseDateList" item="item" separator="," open="(" close=")">
                        #{item}
                    </foreach>
                </if>
                <if test="condition.statusList!=null and condition.statusList.size()>0">
                    and bd.status in
                    <foreach collection="condition.statusList" item="item" separator="," open="(" close=")">
                        #{item}
                    </foreach>
                </if>
                <if test="condition.submitManIdList!=null and condition.submitManIdList.size()>0">
                    and bd.submit_man_id in
                    <foreach collection="condition.submitManIdList" item="item" separator="," open="(" close=")">
                        #{item}
                    </foreach>
                </if>
                <if test="condition.receiveManIdList!=null and condition.receiveManIdList.size()>0">
                    and bd.receive_man_id in
                    <foreach collection="condition.receiveManIdList" item="item" separator="," open="(" close=")">
                        #{item}
                    </foreach>
                </if>
                <if test="condition.deptIdList!=null and condition.deptIdList.size()>0">
                    and bd.dept_id in
                    <foreach collection="condition.deptIdList" item="item" separator="," open="(" close=")">
                        #{item}
                    </foreach>
                </if>
                <if test="condition.bizDemandIds!=null and condition.bizDemandIds.size()>0">
                    and bd.id not in
                    <foreach collection="condition.bizDemandIds" item="item" separator="," open="(" close=")">
                        #{item}
                    </foreach>
                </if>
                <if test="condition.copier!=null and condition.copier!='' ">
                    and bd.id IN (
                    SELECT p.main_id
                    FROM person p
                    WHERE p.user_id = #{condition.copier}
                    AND p.type = 30
                    AND p.is_deleted = false
                    )
                </if>
                <if test="condition.createDateStart!=null and condition.createDateEnd!=null">
                    and bd.create_date &gt;= #{condition.createDateStart}
                    and bd.create_date &lt;= #{condition.createDateEnd}
                </if>
                <if test="condition.projectEndDateStart != null and condition.projectEndDateEnd != null">
                    and bd.project_end_date is not null
                    and bd.project_end_date &gt;= #{condition.projectEndDateStart}
                    and bd.project_end_date &lt;= #{condition.projectEndDateEnd}
                </if>
                <if test="condition.sourceId != null and condition.sourceId != ''">
                    and bd.source_id = #{sourceId}
                </if>
                <if test="condition.customerDevDemand != null">
                    and bd.customer_dev_demand = #{condition.customerDevDemand}
                </if>
                <if test="condition.hopeReleaseDayStart != null">
                    and bd.hope_release_day >= date(#{condition.hopeReleaseDayStart})
                </if>
                <if test="condition.hopeReleaseDayEnd != null">
                    and bd.hope_release_day &lt;= date(#{condition.hopeReleaseDayEnd})
                </if>
                <if test="condition.productSolution != null and condition.productSolution != ''">
                    and bd.product_solution like concat('%', #{condition.productSolution}, '%')
                </if>
                <if test="condition.srExpertIds != null and condition.srExpertIds.size() > 0">
                    AND bd.sr_expert_id IN
                    <foreach collection="condition.srExpertIds" item="item" separator="," open="(" close=")">
                        #{item}
                    </foreach>
                </if>
                <if test="condition.affectCustomerOrder != null">
                    and bd.affect_customer_order = #{condition.affectCustomerOrder}
                </if>
                <if test="condition.stuckOrderInstructions != null and condition.stuckOrderInstructions != ''">
                    and bd.stuck_order_instructions like concat('%', #{condition.stuckOrderInstructions}, '%')
                </if>
                <if test="condition.customerGrade != null and condition.customerGrade!=''">
                    and bd.customer_grade = #{condition.customerGrade}
                </if>
                and bd.is_deleted = false
            </if>
            <if test="parentCondition != null">
                <if test="parentCondition.targetCustomer != null and parentCondition.targetCustomer != ''">
                    and bd.target_customer = #{parentCondition.targetCustomer}
                </if>
                <if test="parentCondition.customerGrade != null and parentCondition.customerGrade!=''">
                    and bd.customer_grade = #{parentCondition.customerGrade}
                </if>
                <if test="parentCondition.priority!=null">
                    and bd.priority = #{parentCondition.priority}
                </if>
                <if test="parentCondition.productLineId!=null">
                    and bd.product_line_id = #{parentCondition.productLineId}
                </if>
                <if test="parentCondition.bizDomainId!=null">
                    and pl.biz_domain_id = #{parentCondition.bizDomainId}
                </if>
                <if test="parentCondition.receiveManId!=null">
                    and bd.receive_man_id = #{parentCondition.receiveManId}
                </if>
                <if test="parentCondition.status!=null">
                    and bd.status = #{parentCondition.status}
                </if>
                <if test="parentCondition.labelCategoryIds!=null and parentCondition.labelCategoryIds.size()>0">
                    and f.label_category_id in
                    <foreach collection="parentCondition.labelCategoryIds" item="item" separator="," open="(" close=")">
                        #{item}
                    </foreach>
                </if>
                <if test="parentCondition.deptId!= null">
                    and bd.dept_id = #{parentCondition.deptId}
                </if>
            </if>
            <if test="groupField!= null and groupField!='' ">
                group by ${groupField}
            </if>
            <if test="orderField!= null and orderField!='' ">
                order by ${orderField}
            </if>
        </where>
    </select>

    <select id="getGroupList" resultType="com.timevale.forward.dal.entity.BizDemandListDO">
        select bd.*,
        pl.id productLineId, pl.name productLineName,
        b.id bizDomainId , b.name bizDomainName
        from biz_demand bd
        left join product_line pl on pl.id = bd.product_line_id
        left join biz_domain b on pl.biz_domain_id = b.id
        <where>
            <if test="condition != null">
                <if test="condition.id!=null">
                    and bd.id = #{condition.id}
                </if>
                <if test="condition.containIds!=null and condition.containIds.size()>0">
                    and bd.id in
                    <foreach collection="condition.containIds" item="item" separator="," open="(" close=")">
                        #{item}
                    </foreach>
                </if>
                <if test="condition.exclusiveIds!=null and condition.exclusiveIds.size()>0">
                    and bd.id not in
                    <foreach collection="condition.exclusiveIds" item="item" separator="," open="(" close=")">
                        #{item}
                    </foreach>
                </if>
                <if test="condition.name!=null and condition.name!='' ">
                    and bd.name like concat('%',#{condition.name},'%')
                </if>
                <if test="condition.desc!=null and condition.desc!='' ">
                    and bd.`desc` like concat('%',#{condition.desc},'%')
                </if>
                <if test="condition.targetCustomer!=null and condition.targetCustomer!='' ">
                    and bd.target_customer like concat('%',#{condition.targetCustomer},'%')
                </if>
                <if test="condition.priorityList!=null and condition.priorityList.size()>0">
                    and bd.priority in
                    <foreach collection="condition.priorityList" item="item" separator="," open="(" close=")">
                        #{item}
                    </foreach>
                </if>
                <if test="condition.bizDomainIdList!=null and condition.bizDomainIdList.size()>0">
                    and pl.biz_domain_id in
                    <foreach collection="condition.bizDomainIdList" item="item" separator="," open="(" close=")">
                        #{item}
                    </foreach>
                </if>
                <if test="condition.productLineIdList!=null and condition.productLineIdList.size()>0">
                    and bd.product_line_id in
                    <foreach collection="condition.productLineIdList" item="item" separator="," open="(" close=")">
                        #{item}
                    </foreach>
                </if>
                <if test="condition.planReleaseDateList!=null and condition.planReleaseDateList.size()>0">
                    and bd.plan_release_date in
                    <foreach collection="condition.planReleaseDateList" item="item" separator="," open="(" close=")">
                        #{item}
                    </foreach>
                </if>
                <if test="condition.statusList!=null and condition.statusList.size()>0">
                    and bd.status in
                    <foreach collection="condition.statusList" item="item" separator="," open="(" close=")">
                        #{item}
                    </foreach>
                </if>
                <if test="condition.submitManIdList!=null and condition.submitManIdList.size()>0">
                    and bd.submit_man_id in
                    <foreach collection="condition.submitManIdList" item="item" separator="," open="(" close=")">
                        #{item}
                    </foreach>
                </if>
                <if test="condition.receiveManIdList!=null and condition.receiveManIdList.size()>0">
                    and bd.receive_man_id in
                    <foreach collection="condition.receiveManIdList" item="item" separator="," open="(" close=")">
                        #{item}
                    </foreach>
                </if>
                <if test="condition.deptIdList!=null and condition.deptIdList.size()>0">
                    and bd.dept_id in
                    <foreach collection="condition.deptIdList" item="item" separator="," open="(" close=")">
                        #{item}
                    </foreach>
                </if>
                <if test="condition.bizDemandIds!=null and condition.bizDemandIds.size()>0">
                    and bd.id not in
                    <foreach collection="condition.bizDemandIds" item="item" separator="," open="(" close=")">
                        #{item}
                    </foreach>
                </if>
                <if test="condition.copier!=null and condition.copier!='' ">
                    and bd.id IN (
                    SELECT p.main_id
                    FROM person p
                    WHERE p.user_id = #{condition.copier}
                    AND p.type = 30
                    AND p.is_deleted = false
                    )
                </if>
                <if test="condition.createDateStart!=null and condition.createDateEnd!=null">
                    and bd.create_date &gt;= #{condition.createDateStart}
                    and bd.create_date &lt;= #{condition.createDateEnd}
                </if>
                <if test="condition.projectEndDateStart != null and condition.projectEndDateEnd != null">
                    and bd.project_end_date is not null
                    and bd.project_end_date &gt;= #{condition.projectEndDateStart}
                    and bd.project_end_date &lt;= #{condition.projectEndDateEnd}
                </if>
                <if test="condition.sourceId != null and condition.sourceId != ''">
                    and bd.source_id = #{sourceId}
                </if>
                <if test="condition.customerDevDemand != null">
                    and bd.customer_dev_demand = #{condition.customerDevDemand}
                </if>
                <if test="condition.hopeReleaseDayStart != null">
                    and bd.hope_release_day >= date(#{condition.hopeReleaseDayStart})
                </if>
                <if test="condition.hopeReleaseDayEnd != null">
                    and bd.hope_release_day &lt;= date(#{condition.hopeReleaseDayEnd})
                </if>
                <if test="condition.productSolution != null and condition.productSolution != ''">
                    and bd.product_solution like concat('%', #{condition.productSolution}, '%')
                </if>
                <if test="condition.srExpertIds != null and condition.srExpertIds.size() > 0">
                    AND bd.sr_expert_id IN
                    <foreach collection="condition.srExpertIds" item="item" separator="," open="(" close=")">
                        #{item}
                    </foreach>
                </if>
                <if test="condition.affectCustomerOrder != null">
                    and bd.affect_customer_order = #{condition.affectCustomerOrder}
                </if>
                <if test="condition.stuckOrderInstructions != null and condition.stuckOrderInstructions != ''">
                    and bd.stuck_order_instructions like concat('%', #{condition.stuckOrderInstructions}, '%')
                </if>
                <if test="condition.customerGrade != null and condition.customerGrade!=''">
                    and bd.customer_grade = #{condition.customerGrade}
                </if>
                and bd.is_deleted = false
            </if>
            <if test="parentCondition != null">
                <if test="parentCondition.targetCustomer !=null and parentCondition.targetCustomer!=''">
                    and bd.target_customer = #{parentCondition.targetCustomer}
                </if>
                <if test="parentCondition.customerGrade != null and parentCondition.customerGrade!=''">
                    and bd.customer_grade = #{parentCondition.customerGrade}
                </if>
                <if test="parentCondition.priority!=null">
                    and bd.priority = #{parentCondition.priority}
                </if>
                <if test="parentCondition.productLineId!=null">
                    and bd.product_line_id = #{parentCondition.productLineId}
                </if>
                <if test="parentCondition.bizDomainId!=null">
                    and pl.biz_domain_id = #{parentCondition.bizDomainId}
                </if>
                <if test="parentCondition.receiveManId!=null">
                    and bd.receive_man_id = #{parentCondition.receiveManId}
                </if>
                <if test="parentCondition.notInReceiveManIds!=null and parentCondition.notInReceiveManIds.size()>0">
                    and bd.receive_man_id not in
                    <foreach collection="parentCondition.notInReceiveManIds" item="item" separator="," open="(" close=")">
                        #{item}
                    </foreach>
                </if>
                <if test="parentCondition.status!=null">
                    and bd.status = #{parentCondition.status}
                </if>
                <if test="parentCondition.deptId!= null">
                    and bd.dept_id = #{parentCondition.deptId}
                </if>
                <if test="parentCondition.containIds!=null and parentCondition.containIds.size()>0">
                    and bd.id in
                    <foreach collection="parentCondition.containIds" item="item" separator="," open="(" close=")">
                        #{item}
                    </foreach>
                </if>
                <if test="parentCondition.exclusiveIds!=null and parentCondition.exclusiveIds.size()>0">
                    and bd.id not in
                    <foreach collection="parentCondition.exclusiveIds" item="item" separator="," open="(" close=")">
                        #{item}
                    </foreach>
                </if>
            </if>
            <if test="orderField!= null and orderField!='' ">
                order by ${orderField}
            </if>
        </where>
    </select>

    <select id="getSimpleGroupList" resultType="com.timevale.forward.dal.entity.BizDemandGroupFieldDO">
        select bd.id, bd.status, bd.priority, pl.id productLineId, b.id bizDomainId, bd.target_customer, bd.dept_id, bd.receive_man_id,
        e.label_id, f.label_category_id
        from biz_demand bd
        left join product_line pl on pl.id = bd.product_line_id
        left join biz_domain b on pl.biz_domain_id = b.id
        left join biz_label e on e.biz_id=bd.id and e.type = 10 and e.is_deleted = false
        left join label f on f.id = e.label_id and f.is_deleted = false
        <where>
            <if test="condition != null">
                <if test="condition.id!=null">
                    and bd.id = #{condition.id}
                </if>
                <if test="condition.containIds!=null and condition.containIds.size()>0">
                    and bd.id in
                    <foreach collection="condition.containIds" item="item" separator="," open="(" close=")">
                        #{item}
                    </foreach>
                </if>
                <if test="condition.exclusiveIds!=null and condition.exclusiveIds.size()>0">
                    and bd.id not in
                    <foreach collection="condition.exclusiveIds" item="item" separator="," open="(" close=")">
                        #{item}
                    </foreach>
                </if>
                <if test="condition.name!=null and condition.name!='' ">
                    and bd.name like concat('%',#{condition.name},'%')
                </if>
                <if test="condition.desc!=null and condition.desc!='' ">
                    and bd.`desc` like concat('%',#{condition.desc},'%')
                </if>
                <if test="condition.targetCustomer!=null and condition.targetCustomer!='' ">
                    and bd.target_customer like concat('%',#{condition.targetCustomer},'%')
                </if>
                <if test="condition.priorityList!=null and condition.priorityList.size()>0">
                    and bd.priority in
                    <foreach collection="condition.priorityList" item="item" separator="," open="(" close=")">
                        #{item}
                    </foreach>
                </if>
                <if test="condition.bizDomainIdList!=null and condition.bizDomainIdList.size()>0">
                    and pl.biz_domain_id in
                    <foreach collection="condition.bizDomainIdList" item="item" separator="," open="(" close=")">
                        #{item}
                    </foreach>
                </if>
                <if test="condition.productLineIdList!=null and condition.productLineIdList.size()>0">
                    and bd.product_line_id in
                    <foreach collection="condition.productLineIdList" item="item" separator="," open="(" close=")">
                        #{item}
                    </foreach>
                </if>
                <if test="condition.planReleaseDateList!=null and condition.planReleaseDateList.size()>0">
                    and bd.plan_release_date in
                    <foreach collection="condition.planReleaseDateList" item="item" separator="," open="(" close=")">
                        #{item}
                    </foreach>
                </if>
                <if test="condition.statusList!=null and condition.statusList.size()>0">
                    and bd.status in
                    <foreach collection="condition.statusList" item="item" separator="," open="(" close=")">
                        #{item}
                    </foreach>
                </if>
                <if test="condition.submitManIdList!=null and condition.submitManIdList.size()>0">
                    and bd.submit_man_id in
                    <foreach collection="condition.submitManIdList" item="item" separator="," open="(" close=")">
                        #{item}
                    </foreach>
                </if>
                <if test="condition.receiveManIdList!=null and condition.receiveManIdList.size()>0">
                    and bd.receive_man_id in
                    <foreach collection="condition.receiveManIdList" item="item" separator="," open="(" close=")">
                        #{item}
                    </foreach>
                </if>
                <if test="condition.deptIdList!=null and condition.deptIdList.size()>0">
                    and bd.dept_id in
                    <foreach collection="condition.deptIdList" item="item" separator="," open="(" close=")">
                        #{item}
                    </foreach>
                </if>
                <if test="condition.bizDemandIds!=null and condition.bizDemandIds.size()>0">
                    and bd.id not in
                    <foreach collection="condition.bizDemandIds" item="item" separator="," open="(" close=")">
                        #{item}
                    </foreach>
                </if>
                <if test="condition.copier!=null and condition.copier!='' ">
                    and bd.id IN (
                    SELECT p.main_id
                    FROM person p
                    WHERE p.user_id = #{condition.copier}
                    AND p.type = 30
                    AND p.is_deleted = false
                    )
                </if>
                <if test="condition.createDateStart!=null and condition.createDateEnd!=null">
                    and bd.create_date &gt;= #{condition.createDateStart}
                    and bd.create_date &lt;= #{condition.createDateEnd}
                </if>
                <if test="condition.projectEndDateStart != null and condition.projectEndDateEnd != null">
                    and bd.project_end_date is not null
                    and bd.project_end_date &gt;= #{condition.projectEndDateStart}
                    and bd.project_end_date &lt;= #{condition.projectEndDateEnd}
                </if>
                <if test="condition.sourceId != null and condition.sourceId != ''">
                    and bd.source_id = #{sourceId}
                </if>
                <if test="condition.customerDevDemand != null">
                    and bd.customer_dev_demand = #{condition.customerDevDemand}
                </if>
                <if test="condition.hopeReleaseDayStart != null">
                    and bd.hope_release_day >= date(#{condition.hopeReleaseDayStart})
                </if>
                <if test="condition.hopeReleaseDayEnd != null">
                    and bd.hope_release_day &lt;= date(#{condition.hopeReleaseDayEnd})
                </if>
                <if test="condition.productSolution != null and condition.productSolution != ''">
                    and bd.product_solution like concat('%', #{condition.productSolution}, '%')
                </if>
                <if test="condition.srExpertIds != null and condition.srExpertIds.size() > 0">
                    AND bd.sr_expert_id IN
                    <foreach collection="condition.srExpertIds" item="item" separator="," open="(" close=")">
                        #{item}
                    </foreach>
                </if>
                <if test="condition.affectCustomerOrder != null">
                    and bd.affect_customer_order = #{condition.affectCustomerOrder}
                </if>
                <if test="condition.stuckOrderInstructions != null and condition.stuckOrderInstructions != ''">
                    and bd.stuck_order_instructions like concat('%', #{condition.stuckOrderInstructions}, '%')
                </if>
                <if test="condition.customerGrade != null and condition.customerGrade!=''">
                    and bd.customer_grade = #{condition.customerGrade}
                </if>
                and bd.is_deleted = false
            </if>
            <if test="parentCondition != null">
                <if test="parentCondition.targetCustomer != null and parentCondition.targetCustomer != ''">
                    and bd.target_customer = #{parentCondition.targetCustomer}
                </if>
                <if test="parentCondition.customerGrade != null and parentCondition.customerGrade!=''">
                    and bd.customer_grade = #{parentCondition.customerGrade}
                </if>
                <if test="parentCondition.priority!=null">
                    and bd.priority = #{parentCondition.priority}
                </if>
                <if test="parentCondition.productLineId!=null">
                    and bd.product_line_id = #{parentCondition.productLineId}
                </if>
                <if test="parentCondition.bizDomainId!=null">
                    and pl.biz_domain_id = #{parentCondition.bizDomainId}
                </if>
                <if test="parentCondition.receiveManId!=null">
                    and bd.receive_man_id = #{parentCondition.receiveManId}
                </if>
                <if test="parentCondition.status!=null">
                    and bd.status = #{parentCondition.status}
                </if>
                <if test="parentCondition.labelCategoryIds!=null and parentCondition.labelCategoryIds.size()>0">
                    and f.label_category_id in
                    <foreach collection="parentCondition.labelCategoryIds" item="item" separator="," open="(" close=")">
                        #{item}
                    </foreach>
                </if>
                <if test="parentCondition.deptId!= null">
                    and bd.dept_id = #{parentCondition.deptId}
                </if>
            </if>
            <if test="orderField!= null and orderField!='' ">
                order by ${orderField}
            </if>
        </where>
    </select>

    <select id="getSimpleGroupCount" resultType="java.lang.Long">
        select count(distinct bd.id) as total
        from biz_demand bd
        left join product_line pl on pl.id = bd.product_line_id
        left join biz_domain b on pl.biz_domain_id = b.id
        left join biz_label e on e.biz_id=bd.id and e.type = 10 and e.is_deleted = false
        left join label f on f.id = e.label_id and f.is_deleted = false
        <where>
            <if test="condition != null">
                <if test="condition.id!=null">
                    and bd.id = #{condition.id}
                </if>
                <if test="condition.containIds!=null and condition.containIds.size()>0">
                    and bd.id in
                    <foreach collection="condition.containIds" item="item" separator="," open="(" close=")">
                        #{item}
                    </foreach>
                </if>
                <if test="condition.exclusiveIds!=null and condition.exclusiveIds.size()>0">
                    and bd.id not in
                    <foreach collection="condition.exclusiveIds" item="item" separator="," open="(" close=")">
                        #{item}
                    </foreach>
                </if>
                <if test="condition.name!=null and condition.name!='' ">
                    and bd.name like concat('%',#{condition.name},'%')
                </if>
                <if test="condition.desc!=null and condition.desc!='' ">
                    and bd.`desc` like concat('%',#{condition.desc},'%')
                </if>
                <if test="condition.targetCustomer!=null and condition.targetCustomer!='' ">
                    and bd.target_customer like concat('%',#{condition.targetCustomer},'%')
                </if>
                <if test="condition.priorityList!=null and condition.priorityList.size()>0">
                    and bd.priority in
                    <foreach collection="condition.priorityList" item="item" separator="," open="(" close=")">
                        #{item}
                    </foreach>
                </if>
                <if test="condition.bizDomainIdList!=null and condition.bizDomainIdList.size()>0">
                    and pl.biz_domain_id in
                    <foreach collection="condition.bizDomainIdList" item="item" separator="," open="(" close=")">
                        #{item}
                    </foreach>
                </if>
                <if test="condition.productLineIdList!=null and condition.productLineIdList.size()>0">
                    and bd.product_line_id in
                    <foreach collection="condition.productLineIdList" item="item" separator="," open="(" close=")">
                        #{item}
                    </foreach>
                </if>
                <if test="condition.planReleaseDateList!=null and condition.planReleaseDateList.size()>0">
                    and bd.plan_release_date in
                    <foreach collection="condition.planReleaseDateList" item="item" separator="," open="(" close=")">
                        #{item}
                    </foreach>
                </if>
                <if test="condition.statusList!=null and condition.statusList.size()>0">
                    and bd.status in
                    <foreach collection="condition.statusList" item="item" separator="," open="(" close=")">
                        #{item}
                    </foreach>
                </if>
                <if test="condition.submitManIdList!=null and condition.submitManIdList.size()>0">
                    and bd.submit_man_id in
                    <foreach collection="condition.submitManIdList" item="item" separator="," open="(" close=")">
                        #{item}
                    </foreach>
                </if>
                <if test="condition.receiveManIdList!=null and condition.receiveManIdList.size()>0">
                    and bd.receive_man_id in
                    <foreach collection="condition.receiveManIdList" item="item" separator="," open="(" close=")">
                        #{item}
                    </foreach>
                </if>
                <if test="condition.deptIdList!=null and condition.deptIdList.size()>0">
                    and bd.dept_id in
                    <foreach collection="condition.deptIdList" item="item" separator="," open="(" close=")">
                        #{item}
                    </foreach>
                </if>
                <if test="condition.bizDemandIds!=null and condition.bizDemandIds.size()>0">
                    and bd.id not in
                    <foreach collection="condition.bizDemandIds" item="item" separator="," open="(" close=")">
                        #{item}
                    </foreach>
                </if>
                <if test="condition.copier!=null and condition.copier!='' ">
                    and bd.id IN (
                    SELECT p.main_id
                    FROM person p
                    WHERE p.user_id = #{condition.copier}
                    AND p.type = 30
                    AND p.is_deleted = false
                    )
                </if>
                <if test="condition.createDateStart!=null and condition.createDateEnd!=null">
                    and bd.create_date &gt;= #{condition.createDateStart}
                    and bd.create_date &lt;= #{condition.createDateEnd}
                </if>
                <if test="condition.projectEndDateStart != null and condition.projectEndDateEnd != null">
                    and bd.project_end_date is not null
                    and bd.project_end_date &gt;= #{condition.projectEndDateStart}
                    and bd.project_end_date &lt;= #{condition.projectEndDateEnd}
                </if>
                <if test="condition.sourceId != null and condition.sourceId != ''">
                    and bd.source_id = #{sourceId}
                </if>
                <if test="condition.customerDevDemand != null">
                    and bd.customer_dev_demand = #{condition.customerDevDemand}
                </if>
                <if test="condition.hopeReleaseDayStart != null">
                    and bd.hope_release_day >= date(#{condition.hopeReleaseDayStart})
                </if>
                <if test="condition.hopeReleaseDayEnd != null">
                    and bd.hope_release_day &lt;= date(#{condition.hopeReleaseDayEnd})
                </if>
                <if test="condition.productSolution != null and condition.productSolution != ''">
                    and bd.product_solution like concat('%', #{condition.productSolution}, '%')
                </if>
                <if test="condition.srExpertIds != null and condition.srExpertIds.size() > 0">
                    AND bd.sr_expert_id IN
                    <foreach collection="condition.srExpertIds" item="item" separator="," open="(" close=")">
                        #{item}
                    </foreach>
                </if>
                <if test="condition.affectCustomerOrder != null">
                    and bd.affect_customer_order = #{condition.affectCustomerOrder}
                </if>
                <if test="condition.stuckOrderInstructions != null and condition.stuckOrderInstructions != ''">
                    and bd.stuck_order_instructions like concat('%', #{condition.stuckOrderInstructions}, '%')
                </if>
                <if test="condition.customerGrade != null and condition.customerGrade!=''">
                    and bd.customer_grade = #{condition.customerGrade}
                </if>
                and bd.is_deleted = false
            </if>
            <if test="parentCondition != null">
                <if test="parentCondition.targetCustomer != null and parentCondition.targetCustomer != ''">
                    and bd.target_customer = #{parentCondition.targetCustomer}
                </if>
                <if test="parentCondition.customerGrade != null and parentCondition.customerGrade!=''">
                    and bd.customer_grade = #{parentCondition.customerGrade}
                </if>
                <if test="parentCondition.priority!=null">
                    and bd.priority = #{parentCondition.priority}
                </if>
                <if test="parentCondition.productLineId!=null">
                    and bd.product_line_id = #{parentCondition.productLineId}
                </if>
                <if test="parentCondition.bizDomainId!=null">
                    and pl.biz_domain_id = #{parentCondition.bizDomainId}
                </if>
                <if test="parentCondition.receiveManId!=null">
                    and bd.receive_man_id = #{parentCondition.receiveManId}
                </if>
                <if test="parentCondition.status!=null">
                    and bd.status = #{parentCondition.status}
                </if>
                <if test="parentCondition.labelCategoryIds!=null and parentCondition.labelCategoryIds.size()>0">
                    and f.label_category_id in
                    <foreach collection="parentCondition.labelCategoryIds" item="item" separator="," open="(" close=")">
                        #{item}
                    </foreach>
                </if>
                <if test="parentCondition.exclusiveIds!=null and parentCondition.exclusiveIds.size()>0">
                    and bd.id not in
                    <foreach collection="parentCondition.exclusiveIds" item="item" separator="," open="(" close=")">
                        #{item}
                    </foreach>
                </if>
                <if test="parentCondition.deptId!= null">
                    and bd.dept_id = #{parentCondition.deptId}
                </if>
            </if>
        </where>
    </select>
</mapper>
