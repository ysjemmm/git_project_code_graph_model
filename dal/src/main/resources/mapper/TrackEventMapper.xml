<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<!--你接口的包名是com.abc.dao,接口名是NameMapper.java，那么你的mapper.xml的namespace应该是com.abc.dao.NameMapper-->
<mapper namespace="com.timevale.forward.dal.dao.TrackEventMapper">


    <insert id="insert" parameterType="com.timevale.forward.dal.entity.TrackEventDO" useGeneratedKeys="true" keyProperty="id" keyColumn="id">
        insert into track_event (cn_name, eg_name, flow_id,track_map_id,platform,api_name,touch_moment,env,status,create_man ,create_man_id)
        values
        (#{cnName},#{egName}, #{flowId},#{trackMapId},#{platform},#{apiName},#{touchMoment},#{env},#{status},#{createMan},#{createManId})
    </insert>

    <select id="select" resultType="com.timevale.forward.dal.entity.TrackEventDO">
        select id,cn_name, eg_name, flow_id,track_map_id,platform,api_name,touch_moment,env,status,fail_reason,
         is_deleted,create_man ,create_man_id,create_date ,modify_man ,modify_man_id,modify_date
        from track_event a
        <where>
            <if test="id != null">and a.id = #{id}</if>
            <if test="trackMapId != null">and a.track_map_id = #{trackMapId}</if>
            and a.is_deleted=false
        </where>
    </select>

    <select id="list" resultType="com.timevale.forward.dal.entity.TrackEventDO">
        select id,cn_name, eg_name, flow_id,track_map_id,platform,api_name,touch_moment,env,status,fail_reason,
        is_deleted,create_man ,create_man_id,create_date ,modify_man ,modify_man_id,modify_date
        from track_event a
        <where>
            <if test="id != null">and a.id = #{id}</if>
            <if test="trackMapId != null">and a.track_map_id = #{trackMapId}</if>
            <if test="name != null and name != ''">and a.name like concat('%',#{name},'%')</if>
            <if test="status!=null and status.size()>0">
                and a.status in
                <foreach collection="status" item="item" separator="," open="(" close=")">
                    #{item}
                </foreach>
            </if>
            <if test="createManIds!=null and createManIds.size()>0">
                and a.create_man_id in
                <foreach collection="createManIds" item="item" separator="," open="(" close=")">
                    #{item}
                </foreach>
            </if>
            and a.is_deleted=false
        </where>
    </select>

</mapper>

