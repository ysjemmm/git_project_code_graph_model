<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.timevale.forward.dal.dao.BizTimeMapper">
    <insert id="batchInsert">
        INSERT INTO biz_time (main_id, main_type, user_name, status, time)
        VALUES
        <foreach collection="bizTimes" item="item" separator=",">
            (#{item.mainId}, #{item.mainType},#{item.userName}, #{item.status}, #{item.time})
        </foreach>
    </insert>

</mapper>
