<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.timevale.forward.dal.dao.ViewsUserMapper">

    <!-- 根据主键id获取视图 -->
    <select id="get" resultType="com.timevale.forward.dal.entity.ViewsUserDO">
        SELECT * FROM views_user WHERE id=#{id} AND is_deleted = 0
    </select>

    <!-- 根据视图id和用户id获取视图 -->
    <select id="getByViewIdAndOwnerId" resultType="com.timevale.forward.dal.entity.ViewsUserDO">
        SELECT * FROM views_user WHERE views_id=#{viewsId} AND owner_id=#{ownerId} AND is_deleted = 0
    </select>

    <!-- 新增一条视图用户 -->
    <insert id="insert" parameterType="com.timevale.forward.dal.entity.ViewsUserDO" useGeneratedKeys="true" keyProperty="id" keyColumn="id">
        INSERT INTO views_user
        (views_id, `type`, position, hidden, owner, owner_id, create_man_id, create_man)
        VALUES
        (#{viewsId}, #{type}, #{position}, #{hidden}, #{owner}, #{ownerId}, #{createManId}, #{createMan})
    </insert>

    <!-- 更新视图用户 -->
    <update id="update">
        UPDATE views_user
        <set>
            <if test="hidden != null">hidden = #{hidden},</if>
            <if test="position != null">position = #{position},</if>
            <if test="modifyManId != null">modify_man_id = #{modifyManId},</if>
            <if test="modifyMan != null">modify_man = #{modifyMan},</if>
            <if test="modifyDate != null">modify_date = #{modifyDate},</if>
        </set>
        WHERE id = #{id} and is_deleted = false
    </update>

    <!-- 删除视图用户 -->
    <update id="delete">
        UPDATE views_user
        SET is_deleted = true, modify_man_id = #{modifyManId}, modify_man = #{modifyMan}
        WHERE id = #{id} and is_deleted = false
    </update>

    <!-- 删除视图用户 -->
    <update id="deleteByViewsId">
        UPDATE views_user
        SET is_deleted = true, modify_man_id = #{modifyManId}, modify_man = #{modifyMan}
        WHERE views_id = #{viewsId} and is_deleted = false
    </update>

</mapper>