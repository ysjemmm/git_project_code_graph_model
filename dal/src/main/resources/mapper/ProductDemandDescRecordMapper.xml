<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.timevale.forward.dal.dao.ProductDemandDescRecordMapper">
    <insert id="insert">
        insert into product_demand_desc_record (product_demand_id, version, `desc`, create_man_id,
        create_man
        <if test="createDate != null">
            , create_date
        </if>
        )
        values (#{productDemandId}, #{version}, #{desc}, #{createManId}, #{createMan}
        <if test="createDate != null">
            , #{createDate}
        </if>
        )
    </insert>

    <select id="listByProductDemandId" resultType="com.timevale.forward.dal.entity.ProductDemandDescRecordDO">
        select *
        from product_demand_desc_record
        where product_demand_id = #{productDemandId}
          and is_deleted = false
        order by id desc
    </select>
    <select id="countByProductDemandId" resultType="java.lang.Integer">
        select count(*)
        from product_demand_desc_record
        where product_demand_id = #{productDemandId}
          and is_deleted = false
    </select>
    <select id="getLatestByProductDemandId"
            resultType="com.timevale.forward.dal.entity.ProductDemandDescRecordDO">
        select *
        from product_demand_desc_record
        where product_demand_id = #{productDemandId}
          and is_deleted = false
        order by id desc
        limit 1
    </select>

</mapper>
